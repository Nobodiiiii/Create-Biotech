package com.nobodiiiii.createbiotech.content.dingdongchicken;

import net.minecraft.core.BlockPos;

/** Runtime bridge added to Alternate Current nodes only when that optional mod is present. */
public interface AlternateCurrentNodeAccess {

	BlockPos createBiotech$getPos();
}
