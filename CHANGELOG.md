# Create: Biotech 更新日志

> 记录范围：当前维护分支为 `1.20.1`。`0.1.0` 至 `1.2.0` 的历史记录来自 `main` 分支；后续 `1.2.1` 至 `1.3.0` 的既有记录保留在同一份日志中。
>
> 记录方式：版本号作为最终状态边界；每一节比较该版本与上一个版本边界之间的有效差异，再按功能、玩家可感知行为、兼容性和底层实现归纳。提交标题仅作为定位线索，不直接作为 changelog 内容，因此已经在后续提交中回退的尝试性改动和中间结果不单独列出。

# 0.1.0

> 版本边界：项目初始提交 `eff9154d`（2026-05-02）。这是 `main` 上的初始工程基线，尚未包含可游玩的生物科技内容。

## 工程与运行基础

- 建立 Forge 1.20.1 模组工程，模组 ID 为 `create_biotech`，目标 Java 版本为 17；加入 Gradle Wrapper、资源处理、源码包和源码 JAR 配置，以及 `META-INF/mods.toml`、`pack.mcmeta` 等模组元数据。
- 初始依赖边界固定为 Forge 47.1.33、Create 6.0.8-291、Registrate MC1.20-1.3.3、Flywheel 1.0.5、Vanillin 1.0.0 和 Ponder 1.0.91。模组要求 Minecraft 1.20.1、Forge 47 以上，并要求 Create 6.0.0 以上；客户端和服务端均可加载。
- 建立最小模组入口类和构建时资源展开流程，初始入口只注册模组本身，没有注册方块、物品、配方或实体处理逻辑。

## 本地测试启动器

- 新增 `test.py` 作为本地 Forge 快速启动脚本：读取 Gradle 属性，解析版本 JSON 和运行时 classpath，处理操作系统/特性规则，选择世界并启动 `1.20.1-Forge`。
- 启动前可自动构建项目、把最新模组 JAR 复制到 `.minecraft/mods`，并支持指定实例、世界、窗口尺寸、跳过构建和跳过复制等参数；默认窗口为 1600×900。
- 初始项目描述为“面向 Create 的生物科技机械，起点是垂直物品传送带”，但该版本实际上只有工程骨架，尚无玩家可获得的玩法内容。

# 1.0.1

> 版本边界：`0.1.0` → `1.0.1`（`eff9154d` → `7bfe8c28`，2026-05-27）。这是第一个完整的玩法版本；以下记录的是该边界的最终文件状态。

## 内容注册与基础交互

- 从只有工程骨架的初始版本扩展为完整的 Create 附属模组：新增方块、物品、方块实体、实体、流体、粒子、声音、配方类型、配方序列化器、创造模式物品栏、标签、战利品表、模型、纹理、语言和 Ponder 场景。
- 本版本首次加入的主要内容包括：经验系统、史莱姆传送带、岩浆怪传送带、动力传送带、爆破室、唤魔者附魔室、鱿鱼打印机、生物打包机、纸箱、恶魂热气球、缓冲垫、蜘蛛装配台、固定的钓竿、薛定谔的猫、骨制棘轮、史莱姆离合器、史莱姆万向节、半截传动杆和液态活体史莱姆。
- 大多数机械方块都接入 Create 的扳手、动力、红石、物品能力、漏斗/隧道、护目镜和 Ponder 交互；对应的 JEI 和 Jade 信息同时补齐，玩家可以从配方、状态和结构提示中理解其使用条件。

## 经验系统

### 经验物品与经验母岩

- 新增 `经验粒`、`经验结晶器`、`经验母岩`、`小型经验芽`、`中型经验芽`、`大型经验芽` 和 `经验簇`。一个 Create 经验粒代表 3 点经验；经验结晶器保留为兼容入口，但提示其后续替代物为经验母岩。
- 经验结晶器可以接收玩家或网络中的经验，按 3 点一组缓冲并输出 Create 经验粒；右键或物品能力可以取出已结晶的经验，护目镜和 Jade 会显示其输入/缓冲状态。
- 经验母岩的六个面各自维护独立的经验芽生长状态。输入经验会选择可用面，从小型、中型、大型经验芽逐级成长为成熟经验簇；自然随机刻也能推进成长，输入经验可以加速这一过程。
- 成熟经验簇可以收获为经验簇物品，并触发对应进度；非精准采集会改为生成经验球，精准采集和含水、朝向、附着面规则则按面独立处理。经验芽和经验簇只能附着在满足条件的坚固面或经验母岩面上。
- 每个经验芽面在成长阶段分别对应 8、16、32 个经验粒，成熟经验簇对应 64 个经验粒；自然成长使用随机机会，避免经验母岩在短时间内瞬间完成全部产出。

### 经验储罐与经验管网

- 新增 `经验储罐`：它沿用 Create 竖直储罐的多方块形式，最大横截面为 3×3、高度为 32 格，每格容量 3000 点经验。多方块形成、拆分、控制器转移、窗口/纯色显示、光照刷新和存档恢复均已实现。
- 经验储罐在容量不足或拆分后无法容纳全部经验时，会把溢出的经验安全生成在世界中；控制器、护目镜和 Jade 会显示当前经验量、容量和多方块状态。
- 新增 `经验管道`、`包覆经验管道` 和 `经验泵`。管道连接、开口、跨端点传输和泵的方向与 Create 流体管网的使用习惯一致，但网络中传输的是经验而不是流体。
- 经验网络会根据端点压力动态重算流向和连接关系，缓存流动进度，并在反向压力、方块替换、连接变化和网络重置时清理旧状态；经验泵会把转速换算为输入/抽取压力，并优先自动连接可用的经验端点。
- 经验泵的效率随转速增加，默认按每 RPM 每秒 `3/4` 点经验计算，并以 256 RPM 作为速度归一化参考；经验储罐、经验母岩、经验结晶器和唤魔者附魔室都可以成为网络端点。
- 增加经验管网的管壁、液面/流动可视化、泵渲染、经验球客户端渲染和状态同步；JEI 会显示经验相关数值，Jade 会显示储罐和方块中可用的经验量。

### 经验与配方

- 经验芽和经验簇可通过 Create 粉碎配方分别产出 8、16、32、64 个经验粒，处理时间随成熟度增加；经验管道、经验储罐和经验泵也各有对应的合成配方。
- 经验系统被接入鱿鱼打印机和唤魔者附魔室：附魔总等级会同时决定耗时和经验消耗，经验可以从玩家、经验储罐或经验泵网络中流转。

## 纸箱、生物捕获与盆实体加工

### 纸箱与捕获规则

- 新增 `小型纸箱` 和 `大型纸箱`。纸箱会保存被捕获生物的完整实体 NBT、实体类型、描述和生命值；释放时在点击位置或相邻位置生成生物，并恢复其 AI、无重力等被临时修改的状态。
- 小型纸箱可以捕获小型史莱姆/岩浆怪（尺寸为 1），以及猫、蝙蝠、鸡、兔子、蠹虫、末影螨、蜜蜂、鹦鹉、悦灵、青蛙、豹猫、恼鬼等允许的轻量生物；潜行右键纸箱可以释放其中的生物。
- 大型纸箱默认只允许创造模式玩家直接右键捕获 Mob；除此之外，玩家把大型纸箱放在副手时，可以在受到致命伤害的瞬间捕获攻击过程中的 Mob，取消该次死亡并保存目标。该行为受配置项控制。
- 纸箱切割配方支持 Create 纸板块和绑定纸板：50 tick 可得到两个小型纸箱或一个大型纸箱；捕获和释放都会触发对应进度，纸箱损坏、容器拆除和移动结构解体时不会静默吞掉生物或物品。

### 生物打包机与移动结构

- 新增 `生物打包机`。它是一个有方向的红石机械：通电后播放 20 tick 的打包动画，可以从相邻容器自动取出纸箱、向上释放捕获生物，再把空纸箱送回目标容器；比较器会反映占用和动画状态。
- 生物打包机可以手动接收已装载生物的纸箱，也会在生物释放后处理空纸箱的回收；方块被破坏或中途停止时，正在处理的纸箱会回到容器或掉落，避免内容丢失。
- 为 Create 移动结构增加生物捕获处理器：碾压、钻头、滚轮、锯和结构撞击造成的致命伤害都会在移动结构上下文中被识别，并优先从结构挂载的存储中寻找合适尺寸的空纸箱。
- 移动结构捕获会在服务器端记录 20 tick 的延迟操作，再把填充纸箱放回结构存储或安全掉落；结构解体时会释放已捕获生物或掉落纸箱，并清理客户端动画和追踪状态。

### 盆中的实体加工

- 新增盆实体加工配方类型，支持 `pressing` 和 `mixing` 两种操作、实体类型/尺寸/NBT 条件、物品输出和流体输出；机械压机和机械搅拌机通过 Mixin 接入实体配方筛选。
- 盆会扫描其中的非玩家生物，并把可加工目标锁定在盆中心；史莱姆必须先装入捕获状态才能参与配方。捕获的小型史莱姆以虚拟盆物品槽呈现，最多支持四只，不会被普通手持渲染逻辑误取。
- 漏斗可以向盆中插入或从盆中取出小型史莱姆；被放入盆中的史莱姆会临时设置为 NoAI、无重力并保存原始标志，盆每 10 tick 校验一次目标，配方失效或盆被拆除时会释放它们。
- 新增配方：混合一只尺寸合适、已捕获的史莱姆得到 `250 mB` `液态活体史莱姆`；压制两根 `半截传动杆` 和一只小型史莱姆得到 `史莱姆万向节`。

## 爆破室与防爆物品保险库

### 结构与装配

- 新增 `爆破室核心`、`防爆机壳`、`防爆玻璃`、`防爆边框玻璃`、`防爆物品保险库` 和爆破室相关的结构、动画与 Ponder 内容。
- 爆破室支持 3×3、4×4、5×5 三种占地尺寸，结构高度为四格；外框由防爆机壳/核心构成，内部保持有效的空腔，顶部中心放置朝向正确的 `动力冲压机`，底部中心放置 `生物打包机`。
- 结构中必须存在一个核心和两个完全位于结构内部、互相连接的防爆物品保险库。顶部边缘会自动布置 `链式传动箱`；使用扳手可以在机壳和链式传动箱之间切换，同时保留足够的链式传动箱供冲压机获得动力。
- 结构形成、拆除、断裂和重新连接时都会同步方块状态、链式传动箱动力、保险库输入/输出角色、打包机朝向和正在处理的苦力怕；结构失效会释放受管理的生物并取消待拆包任务。

### 普通爆破与高压聚爆

- 输入保险库只接受装有苦力怕的 `大型纸箱`，会区分普通苦力怕和高压/带电苦力怕，防止两种处理链混用；最多同时管理 9 只苦力怕，具体上限随爆破室尺寸为 1、4、9。
- 打包机按队列把纸箱中的苦力怕送入腔体，苦力怕在入口动画阶段被标记、设置为 NoAI 并由爆破室控制；动力冲压机驱动处理周期，输出会在确认出口保险库有足够空间后再生成。
- 普通苦力怕逐只消耗输入保险库中的一整组物品，使用 Create 粉碎/研磨配方决定输出；没有有效产出的配方不会先删掉输入。长按保险库可以切换输入和输出角色，输出纸箱会在延迟队列完成后重新打包。
- 带电苦力怕进入高压聚爆路径，使用单物品输入和自定义高压配方。首发配方包括：煤炭有概率产出钻石、苦力怕产出苦力怕头、末影龙产出龙首、经验母岩产出 1～3 个经验粒并有概率产出经验母岩、猪灵产出猪灵头、骷髅/凋灵骷髅/僵尸分别产出对应头颅。
- 高压配方支持物品和流体条件、捕获实体条件、处理热量/持续时间、最多八种输出、随机数量区间和独立概率；这套序列化器也是后续扩展特殊实体加工的基础。

### 过载与安全处理

- 爆破室的正常动力上限为 128 RPM。过载点按超出速度累积，并在停止过载时逐步衰减；达到上限会破坏结构并产生爆炸，爆炸强度按有效处理数量计算。
- 高压聚爆使用更高的过载倍率，并在结构中心和角落产生多点爆炸；正在工作和已停止的冲压机会被统一复位，避免部分工作状态继续吞吐物品。
- 护目镜会显示结构缺失、输入阻塞、输出不足、工作中、过载中和过载百分比等状态；爆破室、保险库和过载事件分别有进度、成就和 Ponder 说明。
- 防爆物品保险库由 Create 物品保险库加坚固板材转化而来，转换时保留原库存，支持多方块拆分、移动结构挂载和与生物打包机的物品能力兼容；防爆玻璃和边框玻璃则由玻璃与坚固板材制作。

## 鱿鱼打印机与唤魔者附魔室

### 鱿鱼打印机

- 新增 `鱿鱼打印机`，由 Create 喷口和捕获的鱿鱼纸箱通过物品应用组装。它带有 1000 mB 水槽，水平侧可放置用于指定附魔的模板书，底面不提供物品能力；比较器会读取其中的水量。
- 传送带上的普通附魔书会按模板复制为 `附魔书（复印本）`。复制书保存 StoredEnchantments 数据，但本身不被当作可直接使用的普通附魔书，提示文本会渲染其复制出的附魔。
- 每个附魔等级消耗 50 mB 水，处理时间默认按每等级 20 tick 计算；总附魔等级越高，所需水量和加工时间越多。打印机支持水不足暂停、过滤器能力、动画、墨水粒子和 Create 传送带处理。
- 鱿鱼打印配方接入 JEI，并会为已注册的附魔及其等级动态展开可用显示；打印完成和首次使用会触发进度。

### 唤魔者附魔室

- 新增 `唤魔者附魔室`，由附魔台和捕获的唤魔者纸箱物品应用组装为双高度方块；下方控制器负责库存和处理，上方代理块负责外观与结构完整性。
- 它接收一张附魔书（复印本），工作期间锁定输入，按每个附魔等级消耗 1000 点经验并处理 40 tick；最多缓存 3000 点经验，可以从经验泵或经验网络中取用。
- 经验不足时暂停，输出端未取走时保留成品；取消或拆除时会退回正在处理的复制书并掉落/保留缓存经验，避免附魔书和经验被吞掉。
- 附魔完成后得到真正可使用的普通附魔书，并配有声音、粒子、护目镜、Jade、JEI 和 Ponder 支持。

## 三类传送带与通用传输层

### 史莱姆传送带

- 新增 `史莱姆传送带` 连接器。它通过两次点击连接同轴传动轴，支持水平、斜向、竖直和侧向几何，并支持含水、滑轮转换和 Create 传送带的动力连接。
- 史莱姆传送带拥有相互独立的正面和背面物品轨道；物品到达末端不会直接掉落，而是转入另一轨道循环。轨道端点、侧面和转角会互相转移，支持水平与竖直/侧向之间的交接。
- 两条轨道分别支持漏斗、隧道、粉碎机等 Create 处理，并处理过滤、插入、抽取、阻塞和占位；传送带上的掉落物、玩家和其他实体具有居中、爬坡、碰撞和拾取规则。
- 大型史莱姆会吞下附近掉落的 Create 传送带连接器，尺寸小于 2 的史莱姆不参与；史莱姆死亡后会掉落 `史莱姆传送带` 连接器并触发“还给我！”。多段传送带拆除时会安全弹出轨道中的物品。

### 岩浆怪传送带

- 新增 `岩浆怪传送带`，同样支持水平、斜向、竖直和侧向的 Create 传送带几何，能够运输物品和实体，并与漏斗、隧道、粉碎机及包裹处理连接。
- 岩浆怪传送带会直接对轨道上的物品执行 Create 爆炸/烧炼类处理，处理时间与鼓风机加工一致；没有鼓风机周围的保护机制，物品停留过久时可能再次被处理。
- 轨道会提供火焰粒子、实体减速和斜坡行为，并对靴子、潜行、阻塞和实体上下带进行处理；大型岩浆怪可以吞下掉落的 Create 传送带连接器，死亡时掉落 `岩浆怪传送带` 连接器并触发“烫手！”。

### 动力传送带

- 新增 `动力传送带`。它可以用一个安山合金把至少两段水平的 Create 传送带转换为动力传送带，保留原有滑轮和连接关系，并在转换时安全弹出当前轨道物品；也可以直接使用连接器构建。
- 水平传送带上的实体移动会产生动力输出，玩家等可控实体、动物和其他非 ItemEntity/ExperienceOrb 实体均可参与；实体速度决定输出 RPM，最强速度作为网络转速，默认约以 24 m/s 对应 256 RPM。
- 动力传送带按每段实体累计应力，默认每 RPM 4 应力、每段最多 1024 应力，并按 10 tick 采样速度；客户端同步移动修正、带面动画和实体离开/进入都已处理。
- 动力传送带可以驱动固定的钓竿吸引动物的玩法，并在实体速度达到条件时触发“你跑不过我你信吗”和“自愿加班”。

### 共用传输实现

- 将传送带的物品轨道抽象为 `BeltSurface`、`BeltSurfaceHost` 和 `StandardItemBeltBlock`，把标准、岩浆怪和史莱姆传送带的轨道坐标、朝向、局部/世界坐标转换、占位、端点转换和内容弹出逻辑统一起来。
- 新增 `FunnelInteractionCore` 及漏斗、隧道、机械臂、端口、粉碎机的解析层，使自定义传送带复用 Create 的漏斗放置倾角、过滤、插入/抽取和实体处理约定，而不是为每种轨道复制一套不兼容逻辑。
- 通过多个 Create 兼容 Mixin 接入漏斗、隧道、盆、粉碎机、传输和渲染路径；热路径尽量使用轨道帧和能力解析，避免普通方块实体调用，降低多段传送带连续运行时的同步和性能开销。

## 蜘蛛装配台与仿生构件

- 新增 `蜘蛛装配台` 和自动创建的 `蜘蛛装配台尾齿轮`。蜘蛛装配台由捕获的蜘蛛、仿生构件和齿轮制作，尾齿轮与主体组成双块机械；任一部分被破坏时会校验另一部分并安全掉落。
- 装配台是水平朝向的动力机器，拥有八个机械臂槽位和八个混合材料槽位。机械臂槽位可放置单槽部署器、压机、锯和喷口；每个材料槽既可放物品，也可作为每臂独立的 1000 mB 流体槽。
- 每个槽位支持过滤和锁定，并提供全部锁定操作；装配台从下方传送带接收工件，一次处理一个目标，优先继续序列组装进度，再按轮询顺序分配机械臂。
- 装配台模拟 Create 的部署、压制、切割和灌注配方，分别计算机械臂对应的应力，消耗物品/流体并保留剩余物；动画、机械音、碰撞粒子、存档、网络同步和完成进度均已接入。
- 新增 `仿生构件` 的序列组装：以黄铜板为起点，依次部署肉、皮革、骨头、线和植物标签，并在循环中加入史莱姆球，形成后续热气球和其他生物机械的材料基础。

## 恶魂热气球与缓冲垫

- 新增 `恶魂热气球装配站`、`恶魂驾驶台` 和 `恶魂热气球`。装配站由 Create 矿车组装器、羊毛和铁板制作，驾驶台由安山合金外壳、拉杆和仿生构件制作。
- 装配站为水平朝向并支持扳手旋转；红石状态变化会让下方绳索式装配站伸出或收回，并寻找固定范围内的锚点结构。满足条件时可以自动吸引附近空闲的恶魂进入隐藏座位。
- 组装流程要求放置装配站、粘合可移动结构、按需要摆放缓冲垫并通电；装配站会加入滑轮磁铁和绳索，移走世界中的结构方块，最后生成由恶魂驾驶的热气球移动结构。
- 玩家可以通过 W/S 前进后退、A/D 转向、空格/Shift 升降、Ctrl 停止，并用 Alt 向附近未通电且可用的装配站靠拢；恶魂乘坐时停用 AI，拆除后恢复原 AI。
- 支持绳索长度、拖拽、加速度、最大速度、输入超时、座位朝向和存档恢复；可以通过靠近未通电装配站的磁铁对接，或用剪刀拆除绳索/磁铁。无效结构、空座位、占用状态和回收失败均有安全兜底。
- 新增 16 种染色 `缓冲垫`（白色至黑色）。羊毛切割 50 tick 产出缓冲垫，未染色/红色羊毛使用默认变体；缓冲垫有方向性的 8/16 体素碰撞形状。
- 缓冲垫在移动结构中提供真实世界碰撞，可以阻挡实体/结构穿透、影响矿车和列车速度、在脱离时推开卡住的实体，并在必要时排除受控实体或龙门结构；首次承受冲击会触发“软着陆”。

## 固定的钓竿与薛定谔的猫

- 新增墙面附着的 `固定的钓竿`。它由安山合金和钓鱼竿制作，可右键放入、替换或取出一件诱饵，并通过物品能力保存诱饵、掉落内容和音效状态。
- 模组在新加入的动物上注入自定义诱导目标：动物会搜索附近拥有匹配食物的固定钓竿，被动力传送带带动的动物到达目标时可以完成对应进度。
- 新增 `薛定谔的猫`，由恶魂之泪、红石火把和捕获的猫制作。它按可配置间隔随机输出红石信号，支持持续信号和 2 tick 脉冲两种模式，侧向滚动可配置 tick/秒/分钟单位；只有朝向输出面会产生信号。

## 史莱姆动力部件

- 新增 `史莱姆万向节`。对两个有效的动力端点各点击一次后，会放置两个带方向的隐形连接点，并通过持久化端点 ID 传递旋转和三维应力；默认允许在约 5×5×5 的范围内连接，并适配移动结构子层地址。
- 万向节会审计两端的互相引用、计算端点朝向和旋转符号；拉伸超过断开距离后，双方会替换为 `半截传动杆`，掉落史莱姆球并重建动力网络。用装有史莱姆的纸箱可以对两根半截传动杆重新修复连接。
- Create 传动轴切割配方 50 tick 产出两根半截传动杆；端点移动、连接断开、世界重载和网络重建时都会校验持久化身份，避免出现只在一端存在的幽灵连接。
- 新增 `史莱姆离合器`。它在未通电且下游过载时切断动力传播，保护其余应力网络；离合器会周期性模拟下游容量，确认安全后自动重新接合，并在首次脱开时触发进度。
- 新增 `骨制棘轮`。它只接受配置的正向旋转，反向理论速度会按创造模式马达容量和网络余量施加卡死应力，使整个应力网络锁定，并在反向输入时触发进度。

## 流体与实体交互

- 新增 `液态活体史莱姆`。它具有类似游泳介质的移动、阻力、上浮和绿色雾效，实体落地时使用史莱姆音效；盆混合配方可以把捕获的小型史莱姆转化为 250 mB 流体。
- 该流体类型、移动交互、粒子/雾效和 Create 流体能力已接入独立的流体注册体系，为后续传送液等跨维度流体内容预留了可复用的交互边界。

## 进度、JEI、Jade 与 Ponder

- 为首发功能新增进度链，覆盖初次进入模组、经验母岩、传送带、动力与过载、纸箱捕获、生物快递、爆破室、蜘蛛装配、热气球和史莱姆部件等目标；包括“海带传动”“换挡，加速，起飞！”“远大包袱”“灵感扩散”“航空学？”“固若金汤”“爆破，轻而易举啊！”“血肉苦弱”“小小的也很可爱”“逮到你了！”等进度。
- JEI 新增经验数值、经验结晶器、经验泵、鱿鱼打印机、唤魔者附魔室、高压配方、实体捕获和传送带转换等分类/配方；动态配方会按注册的附魔和高压输出条件展开。
- Jade 新增方块实体信息提供器，显示经验、纸箱、生物打包机、爆破室、保险库、蜘蛛装配台和气球装配状态；护目镜文本覆盖动力、库存、连接、过载和配方阻塞原因。
- Ponder 为爆破室、纸箱、生物打包机、经验系统、鱿鱼打印机、附魔室、传送带、史莱姆万向节、热气球和缓冲垫补充结构步骤、动力条件、输入输出和失败状态说明。

## 底层重构与兼容性

- 将注册、客户端初始化、网络包、方块实体、菜单、配方、数据生成和渲染职责拆分为可独立维护的模块；服务器状态通过同步包下发到 GUI、渲染器、Ponder 和护目镜，避免客户端自行推测关键处理结果。
- 建立实体捕获的通用 NBT/Ingredient 层、实体恢复和生物尺寸判断，供纸箱、盆加工、爆破室、蜘蛛装配台和热气球共同使用；针对 AI、NoAI、无重力、生命值和死亡回调增加恢复与清理路径。
- 自定义实体配方、随机输出、流体输入、序列组装、移动结构处理和方块实体能力被统一纳入 Create 兼容接口；方块替换、拆除、区块卸载、世界重载和容器满载时优先执行回收、退回或掉落。
- 增加多组 Create Mixin，覆盖传送带、漏斗、隧道、盆、粉碎机、移动结构、机械压机/搅拌机、实体伤害与渲染路径；其中一部分是玩家不可直接感知的坐标转换、轨道占位、网络同步和生命周期重构，但它们构成了后续自定义机械稳定运行的基础。

# 1.0.2

> 版本边界：`1.0.1` → `1.0.2`（`7bfe8c28` → `075962c4`，2026-05-27）。本次没有新增可见玩法，主要是启动时序和发布配置修正。

## 启动与注册时序

- 将 Create 移动结构类型的初始化从模组构造函数延后到 Forge `RegisterEvent` 阶段执行，和其他注册内容的生命周期对齐，避免在注册表尚未准备好时初始化移动结构类型。
- 保留原有方块、物品、实体、粒子、配方和网络注册流程；该调整不改变已有配方或方块行为，目标是降低启动阶段的注册顺序问题。

## 发布配置

- 填入 Modrinth 项目 ID `UW0IjVT0`，使发布脚本能够定位该项目；CurseForge 项目 ID 仍保持待配置状态。

# 1.1.0

> 版本边界：`1.0.2` → `1.1.0`（`075962c4` → `c86d15fb`，2026-06-30）。本节按该边界的最终文件状态归纳；实验性实现、随后被替换或回退的中间结果不单列。

## 经验系统流体化与旧存档兼容

- 删除旧的经验结晶器方块实体、经验储罐、经验物品管线、经验源/汇以及自定义经验管道资源和配方；经验不再依赖一套独立的方块网络。
- 新增与 Create 流体系统兼容的 `experience` 和 `flowing_experience` 虚拟流体，默认具有 15 级亮度和独立经验纹理。普通 Create 流体储罐、管道、开放管道和容器交互都可以承载这种经验流体。
- `ExperienceFluidHelper` 统一识别本模组经验流体以及 Create Enchantment Industry 的 `experience`/`flowing_experience`，负责在经验球、经验值、流体堆栈和兼容容器之间换算，因此两个模组的经验管线可以互相接入。
- 经验泵改为继承 Create 的泵方块和方块实体，使用 Create 的流体传输行为与管道连接状态。泵可以从经验球、玩家、装有 Create 经验粒或成熟经验簇的物品库存，以及兼容的经验储罐/管道中抽取经验；开放输入端会吸引附近的经验球。
- 经验泵的抽取速度改为按动力计算，每 tick 至少抽取 1 mB，通常为 `max(1, |转速| / 2)`；输出端既支持普通流体容器，也支持开放管道。
- 新增开放管道经验效果处理器：本模组经验流体从开放管道流出时，会在服务端生成并合并经验球，让流体化后的经验仍能回到世界中的可拾取经验形态。
- 经验母岩的各面改用经验流体量保存生长和储存状态，并暴露 Forge 流体能力；经验芽、成熟经验簇和经验泵因此能直接参与 Create 的流体自动化。
- 经验芽成长阈值、经验簇数值、经验球拆分以及经验泵相关数值移入服务器配置。1.1.0 默认的小型/中型/大型经验芽数值为 16/32/64 mB，成熟经验簇为 128 mB，每个 Create 经验粒按 3 点经验换算；经验值不再散落在方块实体常量中。
- 增加旧经验储罐 NBT 迁移：读取旧版本的 `StoredExperience`、宽度等字段，并写入 Create 标准 `TankContent`，从旧控制器储罐恢复经验，避免升级后丢失已有经验。
- 唤魔者附魔室的缓存配置改用流体量表示，默认缓存上限调整为 4000 mB；JEI、护目镜、Ponder 和语言文本同步从“经验点”改为 mB/流体管线描述。

## 史莱姆培养皿与拟态生物

- 新增半透明的 `史莱姆培养皿` 方块、方块实体和专用渲染器，并新增 `活体构件` 物品；原先显示为“仿生构件”的物品名称同步改为“活体构件”。配方使用玻璃、不死图腾、皓蓝合金和生化机壳。
- 培养皿可以装入一个活体构件和液态活体史莱姆，并周期性扫描附近可记录的非玩家生物；默认扫描半径为 2 格、每 20 tick 检查一次，盔甲架和旁观者等对象会被排除。
- 记录目标时保存实体类型和最大生命值。默认要求匹配类型的目标继续留在附近，所需液态活体史莱姆量按最大生命值向上取整后乘以每生命值流体量计算，默认每点生命值需要 250 mB，培养皿容量默认 51200 mB。
- 当流体充足且出生空间可用时，培养皿执行 20 tick 的生成动画，消耗活体构件和液体并生成对应生物；界面、护目镜和渲染器会显示构件、记录实体、所需流体和生长/生成状态，渲染器还会先显示史莱姆立方体再过渡到实体尺寸。
- 给所有 LivingEntity 增加持久化且可同步的 `CreateBiotechSlimeMimic` 标记。培养皿生成的生物带有该标记，也可以通过配置开启的刷怪蛋交互创建拟态生物：玩家副手持活体构件并使用刷怪蛋时，符合允许列表/拒绝列表规则的实体会被标记。
- 拟态生物保留再生效果；当它同时受到再生和 Create 灵魂火焰扇的“缠魂”处理时，会执行可配置的 100 tick 拟态解除过程并变回真实生物，跳过 Create 默认的失明、缓慢和马匹转换副作用。
- 拟态生物的受伤和死亡音效改为小型/大型史莱姆音效；默认掉落改为原版史莱姆战利品。拟态村民的交易会改写为史莱姆球交易（默认数量 1–3），取消拟态标记、职业变化或相关状态恢复时会还原原始交易。
- 新增覆盖所有生物渲染器的半透明绿色史莱姆渲染层，并在资源重载时清理缓存；新增培养皿成功和养大幼年拟态生物的进度记录。

## 黄油猫引擎与黄油资源链

- 新增 `趴在传动杆上的可爱猫猫` 和 `黄油猫引擎` 两种动力方块：用装有猫的纸箱通过物品应用制作趴在传动杆上的猫，再用面包对其进行物品应用升级为黄油猫引擎。
- 黄油猫引擎需要猫、面包和黄油才能工作；黄油可以手动放入，也可以由机械臂自动供给。模拟红石信号会反映当前储存的黄油量，拆除时会安全掉落传动杆、面包、猫以及尚未消耗的黄油/超级黄油。
- 默认最多储存 16 份黄油，每 320 tick 消耗 1 份；每份黄油对应 16 RPM，最高输出 256 RPM，动力负载按每 RPM 64 应力计算，最大应力为 16384。黄油衰减、容量、转速和应力均可通过服务器配置调整。
- 普通黄油提供有限的持续动力；超级黄油会将引擎切换为无限供给并保持最大输出。黄油、未完成的超级黄油和超级黄油作为食物时还会提供不同等级的旋转效果；新增“旋转”效果以及旋转/超级旋转药水。
- 增加黄油猫的护目镜、Jade、粒子、声音和客户端旋转表现；Ponder 补充了从纸箱放猫、添加面包和黄油、用更多黄油提高输出、用超级黄油实现持续运转以及分别移动两种方块的流程。
- 新增黄油、奶油、奶油桶、未完成的超级黄油和超级黄油资源与配方。该模块包含从 `SylviaX-390/createbuttercat`（`1.20.1-forge` 分支）改编的部分实现和资源，并补充第三方许可说明。

## 潜影打包机与短距离包裹物流

- 新增 `潜影打包机`、`潜影包裹` 物品、方块实体、专用包裹样式和渲染/动画/声音/粒子资源。用 Create 打包机和装有潜影盒的纸箱进行物品应用即可升级，原有库存、动力、链接和朝向等打包机 NBT 会保留。
- 潜影打包机沿用 Create 打包机的工作、请求、库存、Stock Link、计算机事件和机械臂交互语义；传送中的包裹、输入输出和动画状态会经过占用检查，避免把包裹插入正在工作的目标。
- 打包完成并经过默认 8 tick 的转移延迟后，持有的潜影包裹会自动寻找选定的普通打包机或潜影打包机，并在默认 5 格范围内插入/转移；支持多个输出目标、首次目标、轮询和强制轮询模式，以及包裹请求的顺序和库存元数据。
- 客户端手持潜影打包机物品时，可以右键选择输出打包机、左键取消选择并查看轮廓；扳手可以查看已有的输出选择。机械臂交互点也能参与目标选择，Ponder 增加了短距离潜影物流的搭建、选点和适用范围说明。
- 潜影包裹使用独立的包裹外观，不加入 Create 的随机包裹样式池；新增“消失的包裹”进度记录和 `create:items/packages` 兼容标签。

## 潜影传送站

- 新增由底部、中部、顶部组成的三段式 `潜影传送站`，顶部包含方块实体、菜单和屏幕，中轴为竖直动力结构；配方使用齿轮、安山机壳、两个潜影壳和紫颂果。
- 传送站界面支持设置规范化为小写的本站地址和目标地址，地址最长 32 个字符，并显示最多 64 个候选站点。主世界保存数据会持久化站点的维度、坐标和地址，重载存档后仍能恢复，并优先按同维度距离排序。
- 站点在获得非零动力且触发区域中存在允许的生物或物品实体时启动：执行约 80 tick 的闭合过程和 10 tick 的封闭阶段，将实体传送至地址匹配且空间开放的目标站点。跨维度传送使用服务端维度切换和区块票据，失效或无法加载的站点会从注册表移除。
- 到达站点的实体有默认 80 tick 冷却，防止在两个站点之间立即反弹；传送过程增加传送音效和传送门粒子。
- 三段结构会统一校验；拆除、失效或不完整时三个方块一并安全掉落为一个物品。补充客户端相机偏移、视野裁剪、JEI、GUI、Ponder 和“下一站”进度记录。

## 智能强力胶

- 新增 `智能强力胶`，由电子管、黄铜板和 Create 强力胶制作，具有 99 点耐久；它继承 Create 强力胶能力，同时增加基于连接组的批量选择。
- 手持智能强力胶时会高亮已有的强力胶连接组及其覆盖方块；第一次点击和第二次点击可以选择一段可达的方块范围/胶组，在约 25 格的有效范围内预览矩形区域，服务器复核距离、范围、组成员和库存后消耗一份智能强力胶并创建覆盖选择区域的强力胶实体。
- 普通攻击会移除选中连接组，潜行攻击只移除单个强力胶实体，潜行操作也可取消当前选择；补充选择轮廓、状态提示、音效和 Create 风格粒子。
- 新增 `SmartSuperGlueHelper`，按共享覆盖方块计算连接组；通过 Mixin 扩展 Create 的选择辅助器和客户端输入处理，并通过网络包把创建/移除操作交给服务端验证。

## 末影无线终端

- 新增 `末影无线终端`，由 Create 发射器、末影珍珠和安山机壳制作。右键 Create 仓库管理员/Stock Keeper 后，终端会在物品 NBT 中绑定目标坐标和维度。
- 右键已绑定且仍有效的仓库管理员会打开无线请求菜单；服务端会检查目标存在、仓库管理员身份、权限和访问范围，并同步最近请求摘要、锁定状态和管理员信息。默认访问范围为 32 格，1.1.0 默认不允许跨维度终端访问。
- 终端提示信息显示绑定坐标和维度，并对失效目标给出状态反馈；这为后续的远程/跨维度仓储兼容保留了独立的菜单、权限和网络基础。

## 纸箱、实体和 Create 盆处理重构

- 新增继承 Create `PackageEntity` 的纸箱实体和渲染器。装有生物的纸箱以包裹实体形式掉落或投掷，实体销毁时尽可能释放其中的生物，无法释放时才回退为普通包裹掉落。
- 装有生物的纸箱获得独立的实体模型、图标和 `captured` 模型属性；空纸箱最多堆叠 16 个，装有生物时最多堆叠 1 个，合成消耗后的剩余纸箱会清理捕获数据。新增通用 `RenderedLivingEntityItem` 基础，用于纸箱和捕获小型史莱姆等物品图标的生物渲染。
- 捕获实体辅助器新增 `AiDisabledByMod` 保存标记，在纸箱、盆和移动结构之间转移实体时可以恢复由本模组关闭的 AI，避免实体离开处理设备后保持错误状态。
- 删除旧的自定义 `BasinEntityIngredient`、实体加工配方/操作、盆 Mixin 以及按实体专门处理的液态活体史莱姆配方；改用 Create 标准盆配方输入输出和流体能力。
- 工作盆中的小型史莱姆会在标准 Create 盆库存中表现为虚拟的 `captured_small_slime` 物品，物品数量与实际尺寸为 1 的史莱姆实体同步；只有来自 Create 漏斗或配方路径的调用才会移动这些虚拟物品。盆输出会拦截并恢复实际史莱姆，普通玩家清空盆时不会误取走捕获实体。
- 漏斗现在可以向盆中插入或捕获小型史莱姆；实体离开、失效或处理完成时会释放并恢复 AI/无重力状态，盆会同步对应的虚拟物品数量。新增的小型史莱姆物品可以参与普通 Create 配方：250 mB 液态活体史莱姆压缩成一个捕获小型史莱姆，捕获小型史莱姆混合回 250 mB 液态活体史莱姆。
- 史莱姆万向节配方从旧的实体加工配方迁移到标准 `compacting/universal_joint`，使该配方能使用 Create 的普通盆处理链。

## 新材料、机壳和配方链

- 新增皓蓝合金、皓蓝机壳、生化机壳、锌板、碳粉、石墨、奶油、奶油桶、黄油、未完成的超级黄油和超级黄油等材料、物品和流体。
- 皓蓝合金由 Create 皓蓝矿相关材料与锌粒通过配方获得；新增粉碎锌矿和石头的超热混合，并补齐 Crimsite、Ochrum、Veridium 等 Create 材料的对应冶炼/混合链。
- 木头或去皮木头与皓蓝合金进行物品应用可制成皓蓝机壳，皓蓝机壳再与锌板制成生化机壳；锌锭可压制为锌板。新增 `create:blocks/casing` 和 `create:items/casing` 标签以及机壳连接纹理。
- 煤炭粉碎/研磨制成碳粉，木炭也可制成并带有概率副产；碳粉超热混合制成石墨，碳粉还可以无序合成为黑色染料。
- 牛奶混合成奶油，奶油压缩成黄油；黄油通过三轮序列装配制成超级黄油：依次部署紫颂果/爆裂紫颂果、部署下界之星、注入 250 mB 蜂蜜并压制完成。
- 为新材料补齐资源、模型、语言、JEI、Ponder、标签和 Create 处理配方，并整理机壳连接纹理和模型注册。

## Create 集成、网络和维护性

- 新增统一的客户端、通用和服务器配置注册入口，集中管理经验、爆破室、传送带、培养皿、纸箱、拟态生物、恶魂气球、黄油猫、无线终端、万向节、鱿鱼打印机、唤魔者附魔室、薛定谔的猫、骨制棘轮、盆处理、离合器、液态活体史莱姆、固定钓竿、缓冲垫、潜影打包机和潜影传送站等开关与数值。
- 模组入口改为集中初始化配置和黄油猫模块，并在通用事件阶段注册开放管道经验效果、潜影打包机机械臂交互等处理器；旧的经验管道封装注册路径随经验系统流体化一并移除。
- 动力/史莱姆/岩浆怪传送带的数值和粒子概率改为配置项；史莱姆和岩浆怪传送带接入 Create 的装配操作方块物品识别，能够参与水平装配流程。
- 鱿鱼打印机的 JEI 喷口分类改为完全替换 Create 的绘制逻辑，使用动画鱿鱼、流体和箭头展示打印过程，避免旧的叠加绘制；同步更新 JEI 布局和相关资源。
- 网络协议版本从 2 提升到 5，增加智能强力胶选择/移除、潜影打包机放置/请求、潜影传送站配置等数据包，并补充向指定玩家发送同步数据的接口。
- 清理旧的盆、压缩和混合器自定义 Mixin，加入装配操作、旧经验储罐迁移、史莱姆拟态、智能强力胶、刷怪蛋、村民交易、相机、渲染、JEI、模型和资源重载等新的兼容入口。
- `build.gradle` 将快速游戏启动配置抽成可复用逻辑，保留默认的离线 `quickPlayClient`，并增加显式使用 `--online-build` 的 `quickPlayClientOnline`；`test.py` 默认执行离线构建，也提供 `--online-build` 选项。`gradle.properties` 的模组描述同步从“垂直物品传送带起步”改为更完整的“面向 Create 的生物科技”，发布脚本补充 CurseForge 项目 ID。
- README、中英文玩家介绍、架构说明和功能清单重新整理，内容从“垂直物品传送带起步”扩展为完整的 Create 生物科技模组介绍；许可证改为区分原创资源与第三方改编内容，并新增 `THIRD_PARTY_NOTICES.md`。

# 1.2.0

> 版本边界：`1.1.0` → `1.2.0`（`c86d15f` → `07f3c29`，2026-07-19）

## 悦灵物流系统

- 新增悦灵驿站方块、方块实体、物品、菜单、屏幕、模型、纹理、掉落表和思索内容。悦灵驿站为水平朝向，可使用扳手旋转。
- 悦灵驿站拥有两排九格的包裹槽，以及一个用于放置悦灵快递员的槽位；可存放空的悦灵快递员，并在悦灵快递员装载包裹后派出。悦灵驿站拆除时会处理并掉落其中的悦灵快递员与包裹，避免物品无声丢失。
- 新增悦灵快递员实体、实体物品、空的悦灵快递员和未完成的悦灵快递员。空的悦灵快递员可以堆叠，装载包裹后的悦灵快递员限制为单个；右键可查看或编辑货物地址，潜行右键可直接发射。
- 新增悦灵快递员的序列组装配方：捕获原版悦灵后，依次使用烟花火箭、指南针和 `250 mB` 的传送液加工为悦灵快递员。
- 悦灵驿站可以从下方相邻容器自动提取 Create 包裹，并根据悦灵驿站过滤器决定是否接收；打包机在完成打包后会主动唤醒上方的悦灵驿站，使新包裹能够进入调度流程。
- 悦灵驿站设置界面支持编辑地址（最多 25 个字符）、设置仅接收或仅发送、选择包裹送达后的返回策略，并显示目标端信息。地址匹配支持 Create 包裹的通配规则，也支持用 `@玩家` 明确指定玩家。
- 新增四类物流任务：包裹送给玩家、包裹送到悦灵驿站、悦灵快递员返回悦灵驿站、悦灵快递员返回玩家。目标优先选择精确匹配的悦灵驿站，再选择可用的最近目标；玩家和悦灵驿站目标都会被周期性登记并清理失效记录。
- 悦灵快递员支持跨维度运输。它会经历起飞、离开维度、巡航、降落等阶段，并在目标暂时不可用时重新搜索、等待或按返回策略处理；长时间无法完成时会使用强制到达或失败掉落的兜底路径，避免任务永久卡死。
- 送达后可按悦灵驿站配置始终停靠、始终返回，或仅在目的地无法接收时返回。返回任务带有重试和延迟，最终失败时会把悦灵快递员或包裹安全掉落。
- 新增悦灵任务管理器和 `SavedData` 持久化：任务、任务阶段、悦灵快递员与包裹关系会写入世界存档；实体只在区块可 tick 时生成，并在服务器重启或实体丢失后重新同步，减少重复悦灵快递员和孤儿任务。
- 新增悦灵快递员航迹粒子、悦灵驿站挥动/拍翅反馈、附近问候效果，以及最多显示五条任务的 HUD。HUD 会显示任务状态、预计时间、完成或失败结果，并由服务器定期同步。

## 传送液与传送门

- 新增 `传送液` 源液体、`流动的传送液`、传送液方块、`传送液桶` 和液体类型。它使用紫色传送门风格的视觉效果，具有独立的流动、光照、密度、黏度、雾效和声音参数。
- 原版下界传送门方块现在关联一个 `250 mB` 的虚拟液体储量。Create 流体管道可以抽取该储量；液体被抽空后传送门会消失，并刷新相邻 Create 流体网络。
- 传送门搜索逻辑会把传送液 POI 纳入候选位置，在主世界与下界使用不同搜索半径，并在选定位置时避开墙体边界、保留区块加载票据。
- 进入传送液的实体会使用原版可跨维度实体的传送门处理流程，因此传送液既能作为配方材料，也能作为可实际使用的传送介质。
- 为流体、桶、配方、模型、语言文本和标签补齐数据资源，并将传送液接入悦灵快递员的序列组装链。

## 末影无线终端与 Create 仓储交互

- 末影无线终端改为绑定 Create 的末影人仓库管理员：潜行右键末影人仓库管理员可绑定或重新绑定，普通交互打开已绑定目标；绑定后可从远距离、跨维度访问目标库存。
- 潜行交互其他目标可以清除绑定或显示操作提示；末影无线终端菜单会显示并使用绑定的仓库库存数据，而不是依赖固定方块距离。
- 客户端菜单、远程库存响应和配置包处理进行了配套调整：远程菜单保持打开状态，响应和配置操作会被路由到正确的目标维度，并在目标失效时拒绝无效请求。
- 移除旧的固定距离和跨维度开关逻辑及其对应配置，改为以绑定的末影人仓库管理员是否仍然有效作为访问依据。

## 纸箱与包裹显示

- 小型纸箱和大型纸箱的悬浮提示现在显示包裹地址，并列出最多三个可见内容及其数量；超过显示上限时会给出剩余数量提示，同时跳过生成蛋等不适合作为实体内容展示的条目。
- 对没有实际包裹内容的纸箱增加虚拟自身内容回退，用于维持 Create 包裹和容器交互所需的内容语义；展示列表会过滤掉这条内部回退数据，不把实现细节误显示给玩家。
- 纸箱的固定展示和普通捕获展示使用更合适的物流/捕获模型，改善其在物流场景中的渲染一致性。

## 兼容性、网络与底层调整

- 网络协议扩展以支持悦灵快递员确认、悦灵驿站配置与拍翅反馈、任务 HUD 等新消息；包目标发送方式统一调整，以支持玩家、维度和服务器端任务之间的通信。
- 新增与传送液相关的 POI 注册和服务端 tick/服务器启动接入点，使传送门、悦灵快递员任务和目标登记在正确的服务器生命周期内运行。
- 修复思索世界粒子旋转四元数未归一化导致的异常大粒子问题，尤其覆盖 Sodium/Embeddium 等渲染环境下的表现。
- 移除旧的经验泵 JEI 配方类型、分类和对应语言文本；同步清理已废弃的旧打包机调色板资源和相关说明。
- 补充 Create Phantom、Create Mobile Packages 等第三方依赖的许可证/版权说明，新增中英文权限文档，并更新优化说明、功能介绍和贡献者/依赖信息。

# 1.2.1

> 版本边界：`1.2.0` → `1.2.1`（`07f3c29` → `a56281a`，2026-07-21）

## 迁移到 Minecraft 1.21.1 NeoForge

- 项目运行目标从 Minecraft 1.20.1 Forge 迁移到 Minecraft 1.21.1 NeoForge，版本元数据、加载器范围、Parchment 映射和依赖版本全部同步更新。
- Gradle 构建从 LegacyForge ModDev 插件迁移到 NeoForge ModDev，Java 工具链从 17 升级到 21；新增 GameTest Server、无 GUI 运行参数和 quick play 模组同步配置。
- 模组元数据从 `META-INF/mods.toml` 迁移为模板生成的 `META-INF/neoforge.mods.toml`，同步更新 Mixin 配置、Java 版本声明、refmap 和打包信息。
- Create、Registrate、Flywheel、Vanillin、Ponder、JEI、Jade、MixinExtras 等依赖切换到 1.21.1/NeoForge 对应版本，并调整仓库、运行配置、编译选项和发布配置。
- `publish.py` 新增 1.21.1 NeoForge 分支发布配置和客户端/服务端环境标识，构建产物会带上模组许可证与第三方声明文件。

## 全量 API 适配

- 全部注册系统由 Forge 的 `DeferredRegister`/`RegistryObject` 体系迁移到 NeoForge 的 `DeferredRegister`/`DeferredHolder` 和 1.21.1 内置注册表；资源位置创建方式、方块实体、菜单、流体、物品和实体注册均完成对应适配。
- 菜单类型改用 NeoForge 的菜单扩展接口和 `RegistryFriendlyByteBuf`；流体改用 NeoForge 的 `BaseFlowingFluid` 及其客户端扩展。
- 物品数据从旧式 NBT/Forge 接口逐步接入 1.21.1 的 Data Components；能力、附件、配方成分和配方类型也分别迁移到 NeoForge/1.21.1 对应 API。
- 网络层改用 NeoForge 自定义 payload、`StreamCodec` 和 Create/Catnip 的包注册方式，重新整理服务端与客户端消息的编码、注册和处理入口。
- Ponder、JEI、Jade、Flywheel、移动方块、渲染器、动画、菜单屏幕和各类 Mixin 的方法签名与生命周期全部按 1.21.1 API 更新，原有的生物科技、物流、流体、经验、传送和包装功能随迁移保留。

## 数据资源与运行行为

- 按 1.21.1 数据包命名规则将 `advancements`、`loot_tables`、`recipes`、流体/物品/方块标签等目录迁移为对应的单数路径，并同步调整标签命名空间和 JSON 引用。
- 重新整理 NeoForge 配置注册和客户端/服务端配置结构；原有玩法配置继续由新的配置生命周期加载。
- 为经验泵注册 Create 应力影响值 `4.0`，使其在动力网络中具有明确且稳定的应力消耗表现。
- 更新 1.21.1 下的 Mixin、网络和客户端隔离相关配置，确保原有功能不会因 Forge/NeoForge 类名、注册表或映射变化而在错误侧加载。

# 1.2.1.1

> 版本边界：`1.2.1` → `1.2.1.1`（`a56281a` → `b7546ee`，2026-07-21）

## 双端隔离与依赖加载

- Create 依赖改为不传递加载，避免 NeoForge 环境把不应由本模组直接带入的依赖和侧面代码继续传播给其他运行环境。
- 客户端网络包处理从公共注册代码中移出到独立的客户端处理器；公共端只注册包的结构和路由，具体的动力传送带动画、生物打包机动画、潜影打包机放置、悦灵驿站拍翅和悦灵快递员 HUD 处理仅在客户端执行。
- 为末影人仓库管理员菜单和悦灵驿站菜单的客户端创建入口补充客户端侧标记，避免服务端扫描或加载客户端屏幕类。
- 黄油猫引擎的效果应用改为显式区分客户端与服务端：状态修改、物品消耗、效果和提示只在服务端执行，普通黄油与无限/超级黄油分别正确处理，避免双端重复应用和重复扣除。
- 网络协议版本递增到 13，配合上述客户端处理器拆分，降低专用服务器因解析客户端逻辑而崩溃的风险。

# 1.2.2

> 版本边界：`1.2.1.1` → `1.2.2`（`b7546ee` → `ff553ac`，2026-07-26）

## 巨型青蛙与青蛙胃袋

- 新增四倍体型的巨型青蛙方块和物品。放置时会自动生成一个 `3 × 3 × 2` 的多方块结构，所有部件共享朝向、碰撞体积和破坏逻辑；对任意部件执行破坏都会拆除完整结构并只掉落一个本体。
- 巨型青蛙拥有可见的原版青蛙模型、舌头攻击动画和 Create 风格的物品展示。与传送带相邻时会张嘴，并能把沿动力传送带、史莱姆传送带或岩浆怪传送带送来的物品接入嘴部。
- 巨型青蛙会周期性捕食附近的小型史莱姆；小型史莱姆被吞食后消失并在原位置掉落史莱姆球。被史莱姆套装伪装、蹲下且可以使用传送门的玩家也会被舌头拉入口中。
- 新增独立的青蛙胃袋维度。每只巨型青蛙第一次吞入玩家或接收传送带物品时，会分配一个不会与其他青蛙重叠的私有房间；房间为可修复的空腔立方体，外壳由不可破坏的青蛙胃袋壁组成。
- 每个胃袋房间会生成不可获取的青蛙食道返回传送门。玩家进入青蛙胃袋时记录原维度和精确位置，穿过青蛙食道后返回原处；返回目标不可用时回退到主世界出生点。
- 胃袋房间索引、玩家返回点和巨型青蛙本体之间的关联会写入世界 `SavedData`。巨型青蛙拆下后再放置时会把房间索引写入物品数据，因此不会因搬运而丢失私有空间。
- 服务器配置新增胃袋房间边长设置，默认边长为 48、允许范围为 5～256；房间构建会预加载覆盖区块，并只写入外壳、返回门和必要的空气位置，避免填充整个内部空间。
- 巨型青蛙的传送带输入具有独立的交接动画和时序：会根据传送带速度计算交接时间，输入期间阻止重复捕食或重复接收，无法进入胃袋的剩余物品会在嘴部掉落。

## 史莱姆套装

- 新增史莱姆头盔、史莱姆胸甲、史莱姆护腿和史莱姆靴子。每件装备都可在 Create 工作盆中用对应的纸板头盔、纸板胸甲、纸板护腿或纸板靴子和 `250 mB` 液态活体史莱姆填充获得。
- 史莱姆套装继承 Create 纸板套装的潜行、缩小碰撞体积、怪物隐蔽和头盔视觉效果；穿齐四件并蹲下时，玩家客户端会渲染为小型史莱姆而不是纸箱模型。
- 史莱姆套装的纹理、护甲层、物品模型、JEI/语言提示和客户端纸板潜行覆盖层全部补齐，并作为巨型青蛙识别“可吞食玩家”的前置条件。

## Sable 子空间兼容

- 引入 Sable Companion 1.6.0，并集中新增 `SubLevelCompat` 适配层，统一处理逻辑空间识别、坐标正逆变换、旋转/法线、包围盒、距离、速度和渲染投影。
- 动力传送带、史莱姆传送带、恶魂热气球装配站、潜影打包机、潜影传送站等系统现在能在移动或旋转的 Sable 子空间中工作；实体保持在外部世界投影坐标，方块逻辑仍使用子空间本地坐标。
- 传送带上的玩家移动、表面速度、物品插入、连接、切割和链路遍历都会校验所属逻辑空间，避免不同子空间之间串线或把外部世界实体误当成子空间实体。
- 恶魂热气球的磁铁搜索会同时扫描外部世界和可见子空间，保存目标的子空间 UUID，并把目标移动速度叠加到吸附/停靠过程；停靠、座位实体、乘客朝向、装置拆解和装配位置都改为使用投影坐标。
- 恶魂座位实体现在持久化绑定的装配站位置和子空间身份，更新频率提高到每 tick，使移动子空间中的恶魂和气球不会停留在旧坐标；不再允许从不合法或正在被跟踪的子空间位置装配。
- 潜影传送站的地址记录加入子空间 UUID，目标排序优先同一逻辑空间，再考虑同维度和距离；传送、接收区域、粒子、相机偏移和玩家交互都使用子空间坐标转换，并在区块卸载时注销地址。
- 动力传送带物品会保存首次滑轮所在维度和子空间身份；跨维度、跨子空间或经过不属于同一空间的中间路径时拒绝连接，避免形成不可追踪的动力链。

## 潜影物流与已有系统修正

- 潜影打包机的输出目标改为带有明确逻辑空间身份的目标记录：同空间目标保持 Create 的相对寻址，跨空间目标保存绝对位置和子空间 UUID，并兼容旧版 NBT 位置格式。
- 潜影打包机最多绑定 256 个输出目标，配置时去重、截断超额目标并清除格式错误或空间身份不匹配的记录；输出选择支持首选第一个、轮询和强制轮询模式。
- 新增一次性、带玩家 UUID 和过期时间的服务端配置授权，并捕获客户端预测放置序列；服务端会校验配置修订号、玩家身份、目标数量和实际空间，拒绝过期或伪造的输出配置。
- 潜影打包机传输包裹时会检查目标库存是否真的可以接收，并在插入失败时恢复源库存；目标无效、满载或不兼容时不会吞掉包裹。
- 潜影传送站、潜影打包机和 Sable 子空间新增对应的思索场景、标签、语言文本和数据兼容说明；网络协议追加巨型青蛙捕食和潜影打包机放置结果消息，版本号提升到 15。
- 巨型青蛙、青蛙胃袋壁、青蛙食道、史莱姆套装和相关实体/方块/数据组件完成注册、创造模式标签、模型、纹理、掉落表和配置接入。

## 处理与显示可靠性

- 蜘蛛组装台的处理周期改为“先锁定输入、后在周期中点之后应用一次加工、周期结束再释放”：会持续确认原输入仍在传送带上，输入被替换或消失时取消缓存，避免重复加工、错误加工和重启后状态错乱。
- 蜘蛛组装台将最短处理时长提高为 2 tick，并把处理已应用状态写入存档；加工动画的冲击物品在真正应用配方时确定，而不是仅按时间点播放。
- 爆破室核心客户端维护已加载控制器集合，护目镜查询多方块结构时从已加载控制器反查结构部件，清理失效条目，改善区块边界或结构部件未直接持有控制器时的信息显示。
- 思索世界粒子不再依赖旧的专用 Mixin 路径，客户端世界/相机和子空间投影逻辑统一调整；同步更新思索导出清单、自动生成场景和测试矩阵文档。

# 1.2.2.1

> 版本边界：`1.2.2` → `1.2.2.1`（`ff553ac` → `fca1822`，2026-07-26）

## 蜘蛛组装台

- 当蜘蛛装配台下方两格存在 Create 的置物台（代码中的 `Depot`）时，会扫描其中比当前传送带工件更“先进”的有效工件，并在不破坏当前物品的前提下优先提升高进度序列组装工件；取出的优先工件会被锁定后送入处理流程。
- 优先级只接受确实存在可用下一步配方的工件，普通工件、无效工件和无法继续加工的工件不会抢占传送带；这避免多个序列组装工件混在一起时处理顺序失控。

## 史莱姆传送带与漏斗表面

- 修正竖直史莱姆传送带接入水平回环时的入口方向判断，避免相邻漏斗遮蔽真实的带间交接轨道。
- 物品插入和传送带初始化会即时刷新当前运动方向，因此读档后或动力反转后的坐标换算不会继续使用上一 tick 的方向。
- 在史莱姆传送带接缝处遇到外部或尚未初始化的史莱姆传送带时，物品会被视为硬阻挡，不再错误地绕回自身回环。
- 传送带表面解析器新增使用调用方权威方块状态的入口，碰撞形状查询不再要求临时 `BlockGetter` 能在同一位置提供方块状态；这避免普通漏斗被误套用特殊传送带表面倾斜逻辑。

## 培养皿与维护信息

- 史莱姆培养皿的实体出现空间检查改为直接确认培养皿上方方块为空，修正旧的实体碰撞体预探测在某些实体类型或边界条件下过度拒绝生成的问题。
- 增加史莱姆培养皿思索场景及其导出资源，更新思索注册调用和强力胶场景的资源位置写法。
- README、中英文构建说明和 1.21.1 NeoForge 迁移测试矩阵同步更新为 Minecraft 1.21.1、NeoForge、Java 21 和当前依赖版本。

# 1.2.3

> 版本边界：`1.2.2.1` → `1.2.3`（`fca1822` → `8eada50`，2026-07-30）

## 功德无量全自动放生机

- 新增功德无量全自动放生机的方块、物品、方块实体、独立模型和鱼群/刀片夹具渲染。它继承大型水车的动力行为，因此仍按大型水车的方式接受水流、产生转速和加入应力网络，但外观改为围绕转轮排列的鲑鱼。
- 新增工作台配方：以 8 个装有鲑鱼的纸箱围住 1 个大型水车；配方使用纸箱条件，支持小型纸箱或大型纸箱中的鲑鱼，而不是接受任意普通物品。
- 放置时记录放置者的玩家 UUID。设备只有在确实由水流驱动并产生应力时才累积功德进度；每 60 tick 懒更新一次，支持最高 256 RPM，超过 16 RPM 后采用平方根形式的递减收益，避免单纯堆速度完全线性放大效率。
- 功德进度达到一次脉冲所需的 2 点后，会扫描方块周围 12 格内的村民，并给每个村民对放置者增加 2 点 `MINOR_POSITIVE` 声望；附近没有村民时保留进度并在 200 tick 后重试。任一村民对该玩家的正面声望达到 25 后，授予“功德无量”进度。
- 功德所有者、累计进度、下次扫描时间和按 0.25 点划分的存档检查点都会持久化，重启或区块卸载后不会因为临时 tick 状态而丢失进度；客户端注册专用方块实体渲染和大型水车动画，避免只显示静态方块模型。

## 青蛙胃袋、青蛙消化道与胃袋生态

- 青蛙食道被青蛙消化道取代：房间现在同时拥有高处、预先激活的口部传送门和低处、默认关闭的尾部传送门；两者均为 `3 × 3` 传送门场，并在外侧保留 `5 × 5` 的清空区域，避免生成地形堵住进出口。
- 每个出口外围生成 12 个青蛙消化道壁。对未填充的青蛙消化道壁使用史莱姆球会消耗物品、显示外凸的史莱姆形状并输出满强度模拟信号；当一圈 12 个方块全部填充后，尾部出口才会激活。口部出口在房间创建时直接激活，并使用不同朝向的门框和传送粒子。
- 从口部进入的实体通过巨型青蛙的前方出口离开，从尾部离开的实体通过巨型青蛙的后方出口离开；系统记录巨型青蛙所在维度、精确方块位置和朝向，优先按这条记录返回。旧版按玩家保存的返回点仍作为兼容回退，目标不可用时最终回到主世界出生点。
- 青蛙胃袋房间的基础高度固定为 Y=0，维度环境改为固定时间 13000、环境光为 0、天空光照为 0；青蛙胃袋壁和青蛙消化道壁分别承担不可破坏外壳与可填充出口框架，墙体不再把内部光照完全遮蔽。
- 新增青蛙胃袋分泌物方块：继承蜂蜜块的黏滞移动和滑动手感，但不会参与活塞黏连；落下时不造成摔落伤害，表面显示橙色中空史莱姆壳，并使用对应的滑动/放置音效。新房间会用确定性种子在四角和随机位置生成高度不超过 3、半径不超过 6 的分泌物堆，封闭在分泌物或墙体中的部分偶尔替换为蛙明灯。
- 新房间初始生成 3～5 只史莱姆，优先寻找站在青蛙胃袋分泌物上的位置，不足时再使用墙体地面。青蛙胃袋分泌物上的自然生成候选会移除其他生物，只保留史莱姆，并以固定 50% 的放置概率通过检查。
- 新增类似幽匿催发体的青蛙胃袋分泌物扩散系统：8 格内死亡的史莱姆把其正常经验值转为最近分泌物的扩散电荷，不再直接掉落这部分经验；分泌物会播放蜂蜜方块音效、生成方块粒子，并由最多 32 个持久化游标沿相连分泌物移动，把电荷消耗到有坚实地面的空气方块上。
- 每个扩散游标最多保存 1000 点电荷；找不到可扩散位置时会沿相连分泌物继续寻找，连续停滞达到 32 次后将剩余电荷减半，避免在封闭房间内永久占用 tick。游标、位置、电荷、延迟和停滞计数全部写入世界存档，区块未加载时暂停处理。
- 巨型青蛙的吞入逻辑改为把玩家和物品准确导向口部传送门中心，并施加向房间内部的南向初速度；对应的方块实体、传送门行为、出口位置、声音、粒子类型、纹理、掉落表和客户端透明渲染全部改为青蛙消化道命名。
- 青蛙胃袋房间边长配置的最小值提高到 7（默认 48、最大 256），以保证两端 `3 × 3` 出口、门框和清空区域始终有足够空间；旧房间会清理原先单个地板传送门并迁移到新的双出口结构。

## 液态活体史莱姆的受击采集

- 液态活体史莱姆方块改用专用的动态形状方块实体逻辑，碰撞/选取形状不再依赖静态液体方块；强度设为不可被普通破坏快速摧毁，但仍允许按配置处理受击采集。
- 玩家左击或尝试破坏液态活体史莱姆源液体时，服务端的命中管理器会按方块位置记录命中次数；源液体默认需要 4 次命中，并在第 2、5、8 阶段显示裂纹。达到阈值后清除源液体，并按配置掉落史莱姆球。
- 流动的液态活体史莱姆不使用源液体的多次命中计数，受到有效命中后立即清除；每个服务端维度独立维护命中状态，并在维度卸载时清理，避免同一坐标或旧世界状态串用。
- 旧的通用液体交互处理器不再负责这套命中逻辑，实体在液态活体史莱姆上的弹跳和落地行为保持不变；客户端选取逻辑改由方块自身交互覆盖，移除旧的通用 `GameRenderer` 拦截。

## 史莱姆传送带回环重构

- 新增 `SlimeBeltLoopGeometry`，把史莱姆传送带的前轨道、后轨道、两端转弯、弧长、轨道法线、移动方向和位置/速度互换集中为一套不可变几何；覆盖水平、上坡、下坡、竖直和侧向传送带，并在方块状态或传送带长度变化时自动重新缓存。
- 新增 `SlimeBeltInsertionPlanner`，统一处理漏斗输入、相邻史莱姆传送带接力、传送带捕获和切片恢复。占用检测与实际落点共用同一份插入计划，避免“检测认为空闲、落地却重叠”的分歧。
- 新增 `SlimeBeltNeighbor` 和端点轨道判定，修正水平↔竖直、角落交接、前后轨道连续输入、侧向偏移和回环入口的物品坐标；相邻传送带的运动方向、所属控制器和链路方向不匹配时会阻断交接。
- 物品插入会根据传送方向、来源是否为传送带、轨道侧面和交接类型施加不同的反向偏移；竖直传送带进入水平端点时先落在真实转弯上，再沿回环继续移动，不再跳过转角或穿入已占用物品。
- 重写史莱姆传送带方块实体、物品运输、移动处理、切片和渲染路径：修复回环端点、反向运动、转弯轨迹、竖直/侧向表面、跨带转移、碰撞和重新加载后的状态同步；渲染器按轨道法线分离 Create 前斜姿态、竖直横向基向量和其他表面，改善物品与带面贴合。
- 传送带输入侧的物理方向恢复会优先识别相邻史莱姆传送带，再判断漏斗，避免漏斗遮蔽竖直↔水平带间交接；未初始化或属于其他回环的相邻段被视为硬阻挡，不再错误地把物品绕回当前回环。

## 传送带表面、漏斗和小型史莱姆

- 传送带表面解析改为无状态的表面变换与形状缓存：按朝向缓存表面旋转，分别缓存漏斗轮廓和物品碰撞形状；运行时只接受明确提供特殊表面的动力传送带、史莱姆传送带或岩浆怪传送带，不再把普通传送带或普通漏斗误认为特殊倾斜表面。
- 新增传送带表面提供方块标记，并把放置时的表面扫描与已放置后的运行时解析分开。漏斗的朝向、取放模式、数量过滤、实体目标、抽取方向和世界/局部坐标变换都按实际承载表面计算。
- 漏斗初始放置时会根据特殊传送带表面自动生成对应的安山传送带漏斗或黄铜传送带漏斗，并支持从动力传送带、史莱姆传送带和岩浆怪传送带取放物品；普通 Create 漏斗仍按普通规则工作，避免特殊表面逻辑泄漏到无关方块。
- 漏斗实体接触区域现在会直接捕获进入范围、尺寸为 1 的小型史莱姆并送入工作盆；取放冷却按至少 1 tick 执行，避免在同一 tick 重复捕获。史莱姆被标记为已捕获或已在工作盆中时不会再次进入处理流程。
- 由于已放置普通漏斗的形状自动转换曾导致崩溃，最终版本保留“放置时转换”而关闭运行中的 `updateShape` 自动转换；已有普通漏斗若要变成安山传送带漏斗或黄铜传送带漏斗，需要拆下后重新放置。这是当前版本的明确使用限制。

## 工作盆内的小型史莱姆处理

- 删除旧的 `BasinCapturedSlimeItemHandler`，改用事件驱动的漏斗接触捕获和 `SlimeCaptureFunnelAccess` 接口；工作盆处理器直接把小型史莱姆实体转成捕获物品，并继续支持动态的实体捕获/加工。
- 工作盆的输入输出保护改为只对捕获的小型史莱姆物品生效：外部漏斗、普通能力查询和其他模组不能随意注入、抽取或替换这类实体控制物品，内部加工和合法转移则通过嵌套深度标记明确放行。
- 捕获物品识别改为原始物品身份比较，不再使用可能被其他模组全局替换的 `ItemStack#is` 深层判断；这样不会干扰 One Enough Item 等模组对普通工作盆物品的替换逻辑。

## 手动应用与小型玩法修正

- 新增通用手动应用方向处理器，覆盖除自定义转换外的所有 `ManualApplicationRecipe`：服务端在右击时保存原方块的朝向、轴和面方向，完成变换、拆除、放置、输出和工具消耗；修正手动应用替换方块后朝向丢失或使用客户端预测结果的问题。
- 动力传送带上的玩家行走动画现在把传送带表面位移纳入速度和步态位置计算，按原版 `WalkAnimationState` 的响应系数合并自身行走与带面移动，玩家在站立或被传送带带动时的脚步动画不再停在原地。
- 鱿鱼打印机新增水平朝向属性，放置、扳手旋转、镜像和旋转都会保持朝向；内部鱿鱼模型随方块朝向转动，复制附魔书时不再固定面向一个方向。
- 史莱姆培养皿完成培养时新增“百变怪”“完美伪装”和幼体拟态相关进度的授予，并使用 Sable 子空间投影后的观察位置；黄油猫引擎在黄油数量或效果更新时授予附近玩家对应进度。
- 悦灵驿站自动化改为先把输出包裹推入下方容器，再尝试从下方容器提取新包裹；只有目标容器完整接收后才从驿站移除原包裹，避免满容器导致包裹丢失。玩家包裹成功送达会授予悦灵速递进度，成功派出悦灵快递员会授予使命必达进度。
- 生物打包机配方由原来的十字形多份安山合金结构改为竖直三步结构：安山合金、打包机、安山机壳依次排列；同时补充 Create 打包机兼容进度，使玩家已经获得 Create 打包机进度时可以补发本模组兼容进度。
- 潜影打包机和潜影传送站的相关转换处理提高事件优先级，继续沿用前一版本的空间校验和失败恢复；爆破室核心、潜影打包机、潜影传送站、液态活体史莱姆桶、史莱姆培养皿、鱿鱼打印机和黄油猫等行为补充对应进度触发点。

## 客户端、资源和底层重构

- 客户端新增功德无量全自动放生机、青蛙消化道传送门粒子和史莱姆传送带表面渲染注册；功德无量全自动放生机同时使用大型水车的动力动画和自定义鱼环渲染。
- Create 的自定义方块模型注册从并行客户端初始化移动到首次资源重载之前，并增加一次性保护；这修正 Create 在第一次模型烘焙时快照注册表后，经验泵、安山传送带漏斗、黄铜传送带漏斗、岩浆怪传送带、动力传送带和多种连接纹理模型偶尔缺失的问题。
- 删除旧的 `GameRendererMixin` 液体选取方案，清理对应 Mixin 配置；漏斗、工作盆、传送带表面和液态活体史莱姆的逻辑改为各自的方块/实体交互路径，减少对全局客户端选取的侵入。
- 新增 11 个进度 JSON、史莱姆培养皿和悦灵驿站思索场景、跨维度悦灵运输演示及资源导出；同步补全注册表、创造模式标签、方块状态、模型、纹理、掉落表、粒子和中英文语言文本。
- 网络、资源和注册表同步新增功德无量全自动放生机、青蛙消化道、青蛙消化道壁、青蛙胃袋分泌物及相关客户端数据；同时保留前一版本的 1.21.1 NeoForge 双端隔离结构。

# 1.2.4

> 版本边界：`1.2.3` → `1.2.4`（`8eada50` → `e624aad`，2026-07-31）

## 悦灵驿站的思索场景

- 新增悦灵驿站的完整思索场景，演示从下方打包机或容器提取包裹、按地址派出悦灵快递员、在目标悦灵驿站停靠或返航，以及在地址栏使用 `@玩家` 投递给玩家。
- 场景覆盖普通站点、玩家目标、下界目标和跨维度路线，并明确提示运输没有距离限制但相关站点必须位于已加载区块；场景中的悦灵快递员会实际执行起飞、飞行、进站、拍翅和返航动画，而不是只显示静态示意。
- 新增跨维度悦灵驿站演示资源和基础大型场景资源；悦灵驿站渲染器在思索世界中改用场景当前时间驱动拍翅/挥动动画，离开思索后仍使用正常世界时间，避免演示时间轴与游戏 tick 不同步。
- 移除悦灵驿站旧的悬浮提示条目，把“从下方打包机或容器提取包裹、按地址派送”的说明统一收进思索场景；语言文件只保留悦灵驿站、悦灵快递员及新场景文本的标准译名。

## 思索分类与内容索引

- 新增本模组的思索分类注册：骨制棘轮、史莱姆离合器和史莱姆万向节归入动力传动；动力传送带归入动力源；爆破室核心、岩浆怪传送带、潜影传送站和蜘蛛装配台归入动力装置。
- 唤魔者附魔室、经验泵、史莱姆培养皿和鱿鱼打印机归入流体分类；史莱姆传送带归入物流；悦灵驿站和潜影打包机归入高级物流；薛定谔的猫归入红石；恶魂热气球装配站归入移动锚点。
- 生物打包机和恶魂驾驶台归入移动结构执行器，智能强力胶归入移动结构组装；已有全部导出场景的路径、标题/元数据和索引同步重新生成，使分类页能够直接找到这些内容。

## 传送液与下界传送门兼容修正

- 修正下界传送门方块实体创建条件：只有真正的下界传送门状态才附加传送液储量方块实体，其他共享同一方块类但状态不匹配的情况直接返回空，避免错误附加方块实体。
- 下界传送门的出口注入点继续识别传送液目的地，使普通下界传送门和传送液的流体/传送逻辑不会因状态修正互相污染；传送液兼容逻辑仍保留前一版本的抽取和出口处理。

## 末影无线终端请求界面

- 远程末影无线终端请求界面不再保留物理末影人仓库管理员或烈焰人引用，也不再在关闭界面时调用原版的实体容器关闭逻辑；因此远程请求页面不会因为附近没有实体、实体跨维度或目标已离开而误关窗口。
- 该修正只影响无线终端的客户端请求界面生命周期，实际库存访问仍由绑定的末影人仓库管理员和服务端目标校验负责；同步清理客户端屏幕中的悬空引用，降低远程界面的客户端崩溃风险。

## 资源与维护性

- 更新思索者导出清单、哈希清单、场景资源和中英文语言文本；本版本的运行行为变化集中在下界传送门状态检查、末影无线终端远程界面和悦灵驿站思索演示，其余大量生成文件是同一套场景索引/元数据的最终导出结果。

# 1.2.5

> 版本边界：`1.2.4` → `1.2.5`（`e624aad` → `40e8e5b`，2026-08-02）

## 史莱姆万向节与半截传动杆

- 重做史莱姆万向节的连接模型：连接关系不再依赖世界级端点目录或全局持久化索引，而是由两端方块实体各自保存稳定的端点 UUID、对端地址、对端 UUID、连接对 UUID、维度/子空间身份和移动修订号；这样区块卸载、服务器重启和 Sable 子空间移动都能从本地数据恢复连接。
- 史莱姆万向节物品改为“两次点击连接”：第一次右击有效的实体方块面保存目标，第二次在同一维度、同一逻辑空间且处于起始应变距离内的另一个目标上右击建立连接；跨维度、跨逻辑空间、无效目标或已占用端点会拒绝连接，潜行右击清除选择，成功后有 5 tick 的物品冷却和对应进度。
- 连接创建时会确定唯一的驱动端，按世界轴和端点朝向计算另一端的运动方向与转速修正，并把双方链接作为互相可验证的事务写入；服务端每 8 tick 审计一次互惠关系，发现对端不存在、地址不一致、端点 UUID 不匹配或修订号过期时，会清除失效链接并重建动力学连接，避免单端残留。
- 在 Sable 子空间中，端点移动会递增移动修订号并记录移动前后的地址；受控端迁移到目标位置后，只有检测到相同端点 UUID 且修订号更新的目标已经存在，旧端才会自毁，避免移动过程中生成重复的史莱姆万向节。可选兼容层通过资源存在性检查才启用，不加载 Sable 时不会硬引用其类。
- 跨子空间连接增加弹性物理：距离超过 4.675 格后按平滑三次曲线增加拉力，结合分离阻尼、两端质量、空气阻力和最大冲量限制；达到 5.5 格断开距离时请求断裂。玩家穿过史莱姆万向节连接轴时会按 0.4 倍配置减速，旁观者不受影响。
- 客户端把史莱姆万向节连接的拉伸程度映射到史莱姆离合器过载色，在起始应变距离到断开距离之间显示预览线和粒子；渲染时将 Sable 子空间的局部坐标反投影到当前世界，预览不再因子空间坐标系不同而偏移。
- 过度拉伸时两端会作为一次原子操作同步替换为半截传动杆，保留朝向、含水状态和稳定端点身份；同时在两端平均位置生成一个史莱姆球物品实体，并使用两端平均速度。任一替换步骤失败会回滚连接和方块状态，避免只断一端或重复掉落。
- 半截传动杆现在是可放置的有向方块实体，拥有独立模型、掉落物和客户端可视对象；史莱姆万向节本体使用不可见方块渲染配合自定义动力连接渲染，两个方块都支持含水和方向状态。
- 新增史莱姆万向节修复流程：手持只含一个史莱姆实体的小型纸箱或大型纸箱右击一端半截传动杆，再在连接半径内右击另一端；服务端会核对端点 UUID、地址、连接代数和同一服务器维度，把两端恢复成史莱姆万向节、恢复动力学连接并从纸箱中消耗史莱姆。潜行右击仍优先执行释放纸箱中的生物，并清除修复选择。
- 纸箱中的生物数据访问统一迁移到 `CBItemData`，修复选择、捕获实体清除和箱子释放现在共用同一套数据组件抽象；史莱姆万向节配方和半截传动杆切制配方都受内容功能开关控制。
- 配置补充了史莱姆万向节的起始应变距离、断开距离、峰值张力、分离阻尼、端点空气阻力、最大冲量、轴减速倍率、物品冷却和客户端预览距离；旧的 `maxConnectionRange` 保留为已弃用迁移项，避免已有配置直接失效。

## 青蛙胃袋生态重做

- 新增青蛙胃黏膜方块，作为粉色的活体地形；青蛙胃黏膜、青蛙胃袋壁和青蛙消化道壁都不会造成摔落伤害，胃袋内移动和从高处落下不再惩罚玩家。
- 青蛙胃袋房间改为以世界种子和房间编号为输入的确定性生成：天花板与墙面生成 1～3 格厚的黏膜衬层，地板加入起伏地形，不同房间在相同存档中可稳定复现，且不会因重新加载产生另一套布局。
- 边长至少 12 的房间会生成不规则多瓣水池，通常由 3～5 个半径约 3～8 格的水池瓣组成；水池床位于胃袋地形上方，水面抬高到固定层级，并生成岸边过渡。边长至少 18 的房间还会在六个表面生成 4～8 处多瓣青蛙胃袋分泌物生长体，深度不超过 3 格，内部有概率嵌入蛙明灯或黏液块。
- 大房间会生成 2 + 房间边长/12，再加 0～3 个不规则墙面平台；平台宽约 5～9 格、深约 5～7 格，位于中间高度，并悬挂 3～8 格长的发光浆果洞穴藤蔓，部分藤蔓末端带有蛙明灯。存在水池时，平台会额外生成通向最近池边的瀑布，胃袋不再只是平坦密闭的方盒。
- 新房间会在青蛙胃袋分泌物或墙面地板上生成 3～5 只史莱姆；后续自然生成明确要求落点位于青蛙胃袋分泌物表面，并保留史莱姆专属的生成概率限制。
- 青蛙胃袋分泌物的蔓延从“只寻找地板支撑”改为六方向寻找坚固邻接方块，能够沿墙面、天花板和其他受支持表面扩展；粒子位置和体积同步调整以匹配新的生长面。
- `FrogStomachSpace` 先生成完整生态，再清理两端出口的通道并保留洞穴藤蔓；出生点和史莱姆落点会考虑地形起伏与分泌物厚度，旧的平面分泌物堆生成逻辑被移除。青蛙胃袋壁恢复为不透天光的实体外壳，避免胃袋内部被外界光照穿透。
- 房间大小的最小配置值从 7 提高到 16（默认 48、最大 256），保证水池、平台、生态衬层和两端 3 × 3 出口有足够空间；旧房间继续按新布局迁移并清理遗留的单出口状态。

## 功能开关与配方条件

- 新增统一的内容功能开关系统，以内容家族为粒度控制悦灵驿站、功德无量全自动放生机、生物打包机、活体构件/生化机壳等生物科技材料、骨制棘轮、缓冲垫、黄油猫引擎、纸箱、爆破室核心、唤魔者附魔室、经验、固定的钓竿、青蛙胃袋、恶魂热气球、液态活体史莱姆、岩浆怪传送带、史莱姆培养皿、动力传送带、薛定谔的猫、潜影打包机、潜影传送站、史莱姆套装、史莱姆传送带、史莱姆离合器、智能强力胶、蜘蛛装配台、鱿鱼打印机、传送液、史莱姆万向节和末影无线终端等内容。
- 禁用内容不会从注册表删除，也不会破坏世界里已经存在的方块；但会阻止对应方块、物品、奶油桶、传送液桶和液态活体史莱姆桶的新放置/使用，并阻止其新配方进入服务器配方表。
- 所有本模组配方新增 `neoforge:conditions` 条件；服务器启动前先由统一条件过滤无关配方，服务器即将启动时再依据实际配置执行一次配方过滤，避免世界配方解码早于配置加载而读取错误默认值。修改开关后需要重启或执行 `/reload` 才会刷新配方。
- 新增 `feature_enabled` NeoForge 条件编解码器和统一放置拦截器，内容开关不再分散在各个方块的右击和注册代码中，创造模式物品、配方、手持使用和普通放置行为的判断保持一致。
- 生物打包机的功能开关同时控制移动结构上的致命捕获；禁用时不会在结构中捕获实体，启用时仍会检查大型纸箱的允许/拒绝实体列表，并按小型纸箱优先、允许时再尝试大型纸箱的顺序执行。

## 经验泵、传送液与服务器配置

- 经验泵与 Create Enchantment Industry 的“经验”流体兼容：该模组存在时优先使用其“经验”流体，没有该模组时使用本模组的“经验”流体；内部处理会区分生成的“经验”流体，避免把普通自定义流体误当成经验。
- 经验泵新增独立配置：是否抽取玩家经验、经验球和带经验物品；每 tick 最大传输量；256 RPM 时喷嘴吸引范围（默认 16.5 格，按转速缩放）；以及开放管道是否把“经验”流体重新发射为经验球。流体处理器只接受正确的“经验”流体，设置传输上限后抽取速率也会受限。
- 客户端增加“将经验渲染为流体”的选项，默认仍显示原版经验球；启用后，Create 流体罐中的经验才按普通流体材质渲染，便于与其他流体并排观察。
- 传送液增加是否允许从下界传送门抽取、每个传送门方块对应的流体量以及抽空后是否摧毁传送门方块的配置，默认值保持 250 mB 和抽空摧毁行为。
- 潜影传送站增加跨维度传送、目标区块加载、同空间最大距离、玩家/生物/物品类别、单次最大实体数和到达冷却配置；执行前会先检查目标是否可用，关闭区块加载时要求目标区块已加载，Sable 子空间目标也必须已经加载，不会把无效目标当作外部世界坐标。
- 悦灵物流增加全局任务上限、单玩家任务上限、同维度/同子空间最大投递距离、传送阈值、强制到达阈值、返航重试间隔和返航发射延迟；跨维度或跨子空间路线不受同维度距离限制，但仍受跨维度开关约束，强制到达阈值会自动不小于普通传送阈值。

## 悦灵物流、潜影传送站与兼容性

- 悦灵快递员的派遣在达到任务上限时会明确失败，只有任务真正加入队列才授予使命必达进度；返航队列使用新的延迟与重试配置，玩家返航失败在重试耗尽后才掉落载体，已存储的悦灵快递员载体不会因一次失败丢失。
- 旧返航队列条目会在对应维度/子空间加载后才解析空间身份；如果原空间已不存在或坐标无效，不会把条目错误解释为外部世界的同坐标位置。
- 潜影传送站在单次激活前按配置筛选玩家、生物和物品，并限制输出数量；跨维度/跨空间可用性、目标区块加载和冷却检查统一进入服务端传送事务，减少重复传送和目标未加载造成的实体丢失。
- 纸箱中的生物数据、史莱姆万向节修复和其他需要保存实体信息的路径统一使用新的 `CBItemData` 数据组件；纸箱从旧的原始自定义数据访问迁移后，释放、替换和网络同步使用相同的数据入口。
- Sable 兼容改为可选编译依赖，只有完整 API 资源集存在时才启用兼容 Mixin；空间 UUID 查询、未加载方块实体访问、原始坐标校验和渲染坐标反变换都增加保护，未安装 Sable 时不会因为类加载或反射失败影响主模组。

## 配方、资源与维护性

- 生化机壳配方的锌板从本模组专用物品改为通用 `c:plates/zinc` 标签，兼容其他模组提供的锌板；煤和木炭制碳粉的铣削配方移除，只保留对应的粉碎路线。
- 半截传动杆补齐方块状态、模型、掉落表、注册和切制配方；史莱姆万向节配方、半截传动杆配方以及其他内容的配方条件会随对应功能开关同步生成。
- “经验”流体、传送液、青蛙胃袋、悦灵物流和生物打包机的配置集中迁移到统一 `CBConfigs`，删除旧的分散配置入口；增加配置注释和范围校验，防止任务阈值、房间尺寸、实体传送数量和经验传输量使用非法值。
- 新增史莱姆万向节本地连接设计文档，记录稳定端点身份、Sable 移动修订、断裂/修复事务、回滚策略和不使用世界级端点目录的约束；同步整理优化路线和方块属性/采掘工具规划文档。
- 补充 Create、Sable 和可选兼容路径的事件优先级、注册表、能力挂载、渲染属性和网络处理，确保岩浆怪传送带、动力传送带、史莱姆传送带、潜影传送站及史莱姆万向节在重载和跨空间场景下使用同一套状态校验。

# 1.2.6

> 版本边界：`1.2.5` → `1.2.6`（`40e8e5b` → `603888c`，2026-08-04）

## 方块属性、采掘工具与扳手

- 新增统一的方块属性基线 `CBSharedProperties`：木质类、石质类、软金属类、铜金属类方块分别复用 Create 的共享属性，附魔台和紫水晶各阶段方块复用对应原版属性，玻璃和防爆方块也使用明确的原版基线，减少各方块手写属性造成的硬度、声音、工具和破坏抗性偏差。
- 重新整理史莱姆传送带、岩浆怪传送带、动力传送带、功德无量全自动放生机、唤魔者附魔室、经验泵、经验母岩/经验簇、鱿鱼打印机、史莱姆培养皿、史莱姆万向节、半截传动杆、史莱姆离合器、骨制棘轮、固定的钓竿、恶魂热气球装配站、恶魂驾驶台、蜘蛛装配台、纸箱相关机器和悦灵驿站等方块的材质基线；防爆物品保险库、生化机壳、传送液等现有玩法不变，但挖掘速度、声音、地图颜色、遮挡和正确工具判定更一致。
- 爆破室核心、防爆机壳、防爆物品保险库、防爆玻璃、防爆边框玻璃和防爆结构中的链式传动箱统一使用 1200 的爆炸抗性并要求正确工具；防爆玻璃和防爆边框玻璃加入钻石工具等级标签，钻石镐可直接获得自身掉落，斧头不会被误判为正确工具。
- 将手写的工具标签迁移到 `CBBlockTagsProvider` 数据生成器：补齐斧头可采掘的木质/羊毛类方块、镐可采掘的机器/动力/金属类方块，并生成 `mineable/axe`、`mineable/pickaxe` 和 `needs_diamond_tool` 的最终资源，避免主资源目录和生成资源互相覆盖。
- 新增统一的扳手识别 `CBWrenchHelper`，同时接受 Create 扳手和 NeoForge 通用扳手标签。生物打包机、潜影打包机、培养皿、蜘蛛装配台、悦灵驿站、固定的钓竿等自定义右击方块会先把扳手交还给默认动力交互，不再把扳手误当成普通输入物品。
- 固定的钓竿、恶魂热气球装配站、薛定谔的猫支持用扳手旋转；旋转只在上/下表面点击时执行并检查新状态可存活，保留原有朝向约束。蜘蛛装配台和其他定向动力方块也不再被自定义菜单交互截获。
- 唤魔者附魔室的上下两格现在作为一个完整结构旋转；潜行扳手拆除时先发出可取消的破坏事件，只给出一次下半部分掉落并同步移除上下两格，避免残留半结构或重复掉落。
- 潜影传送站的下、中、上三格同样作为一个完整结构旋转；潜行扳手从任意一格拆除时只生成一次物品并安全移除三格。结构外的链式传动箱不再因默认 `updateAfterWrenched` 被错误替换成防爆机壳。

## 黄油猫引擎的注册与自动化

- 删除独立的黄油猫注册模块及其重复的方块、物品、流体、效果、药水、创造模式标签和机械臂注册入口；趴在传动杆上的可爱猫猫、黄油猫引擎、黄油、未完成的超级黄油、超级黄油和奶油统一迁移到主模组的 DeferredRegister，保留原有注册 ID，降低主模组初始化顺序和客户端类加载风险。
- 黄油猫引擎和趴在传动杆上的可爱猫猫的应力容量、最大转速和动力生成值改为在主模组通用注册阶段集中登记，并从配置的最大转速/最大应力容量计算每 RPM 应力容量；功能开关、配置值和 Create 动力网络保持一致。
- 新增机械臂存取点：机械臂可以向有面包的黄油猫引擎放入黄油、未完成的超级黄油或超级黄油；模拟插入和实际插入共用容量检查，装满时保留物品，带容器的输入正确返回容器。
- 黄油猫引擎的客户端数据与模型映射移到主客户端 `ButterCatPartials`：根据猫变种、是否有面包、黄油数量阶段和是否为无限状态切换猫、面包、绳子、少量/大量黄油或超级黄油模型；Flywheel 可视对象与普通方块实体渲染器都会在状态变化时替换模型，不再由通用方块实体暴露客户端模型对象。
- 方块物品渲染会保存并显示被捕获猫的品种；黄油猫引擎、黄油、未完成的超级黄油、超级黄油和奶油桶补充 Create 风格的动力统计/说明提示，奶油及两种黄油猫方块注册正确的透明或裁剪渲染层。
- 新增 Jade 信息提供器：查看趴在传动杆上的可爱猫猫或黄油猫引擎时显示对应图标；黄油猫引擎会显示当前黄油数量、最大容量、黄油消耗倒计时，超级黄油状态显示无限，并把标题替换为语言文件中的标准名称。
- 黄油猫引擎的思索场景改为使用主模组注册表中的方块和物品，继续演示用装有猫的纸箱生成趴在传动杆上的可爱猫猫、添加面包转为黄油猫引擎、投入黄油提升动力，以及使用超级黄油获得持续动力。

## 黄油、奶油与效果注册

- “奶油”流体的类型和发放行为从旧注册模块拆出为独立内容类：保留不可游泳、不可推动实体、低雾距和流动参数，并把奶油桶的发射器行为在通用初始化阶段注册；当黄油猫内容被禁用时，发射器会保留奶油桶而不强行放置流体。
- 黄油、超级黄油和未完成的超级黄油继续从服务器配置生成饥饿值、饱和度和效果时长；旋转效果迁移到主模组的效果注册表，旋转药水的不同变体迁移到主模组的药水注册表，效果逻辑与原有配方/食用行为保持一致。
- Butter Cat 的食物标签、面包/黄油通用标签和黄油等级判断集中到 `ButterCatIngredients` 与主物品注册表；机械臂、玩家右击和自动化输入不再依赖已删除的独立标签类。

## 维护性与资源生成

- 新增主模组数据生成入口，把方块工具标签纳入统一 `gatherData` 流程；删除旧的手写标签资源，生成结果同时进入 `src/generated/resources`，便于检查工具矩阵和避免重复数据源。
- 统一主模组、客户端、思索、Jade、机械臂、效果和药水的注册生命周期，清理黄油猫模块对通用初始化阶段的客户端模型引用；网络/渲染初始化不再因独立注册器过早触发 Flywheel 模型类加载。
- 补充方块属性、采掘工具、扳手和黄油猫架构说明文档；本版本的行为重点是正确的挖掘/掉落判定、完整多方块扳手操作、黄油猫自动化输入和客户端信息显示，配置与游戏规则本身未被提交标题之外的试验性改动覆盖。

# 1.3.0

> 版本边界：`1.2.6` → `1.3.0`（`603888c` → `b715b1f`，2026-08-17）

## 岩浆怪燃烧室

- 新增岩浆怪燃烧室及空的岩浆怪燃烧室物品。空的岩浆怪燃烧室用黑石、铁锭、铁板和黄铜板制作；使用空的岩浆怪燃烧室右击野生岩浆怪可以捕获它，也可以对展示岩浆怪的刷怪笼执行捕获，不会破坏刷怪笼本身。
- 捕获成功后野生岩浆怪被移除，空的岩浆怪燃烧室变为岩浆怪燃烧室；客户端播放火焰/烟雾，服务端播放岩浆怪受伤和火焰熄灭声音。捕获事件在最高优先级处理，避免原版或其他交互同时消费物品。
- 岩浆怪燃烧室兼容 Create 的烈焰人燃烧室加热接口，可放在工作盆下方作为热源；内部有 1 桶容量的熔岩储罐，只接受熔岩，并按熔岩桶的燃烧时长逐 tick 消耗，保留毫桶级余数而不额外发明燃烧时间。
- 物品放置时根据是否捕获岩浆怪设置为阴燃或无热量状态；通入熔岩后在阴燃、衰退、点燃等状态间切换，低于约 1.25% 剩余燃烧时间时进入衰退。对应的光照、红石比较器输出和工作盆热源状态会随热量同步更新。
- 岩浆怪燃烧室在不燃烧时显示小型岩浆怪跳跃，在燃烧时显示落地/挤压动画；内部熔岩高度按 250 mB 一格渲染，客户端在开始燃烧、持续燃烧和岩浆怪落地时生成火焰、烟雾和粒子。
- 使用动力泵或其他流体能力可以给岩浆怪燃烧室补充熔岩；护目镜会显示剩余燃烧时间（秒）和储罐内容。方块掉落表按无热量、阴燃、衰退、点燃和沸腾状态返回空的或已捕获的岩浆怪燃烧室。
- 新增空的岩浆怪燃烧室到岩浆怪燃烧室的生物手动物品使用配方；同时提供烈焰人燃烧室的手动捕获配方，生物捕获配方统一使用纸箱条件，保持两种燃烧室的使用路径一致。

## 叮咚鸡与实体红石

- 新增叮咚鸡实体、叮咚鸡物品和叮咚鸡刷怪蛋。叮咚鸡继承鸡的体型、行为、繁殖与掉落基础，物品最多堆叠 4 个，丢出多个物品时会在落点周围分散生成多个具有持久性的叮咚鸡。
- 使用呼唤铃右击鸡会将其转换为叮咚鸡；转换会保留位置、速度、年龄、生命值、效果、装备、名称、拴绳、骑乘关系以及其他鸡的持久数据，同时为新实体分配新 UUID，并复制身体旋转和飞行动画状态以避免一帧跳变。已经是叮咚鸡的实体不会重复转换。
- 叮咚鸡普通右击会播放呼唤铃声音并产生持续 20 tick 的红石脉冲；带有语音包时改为播放叮咚鸡语音。右击音符盒会消耗音符盒、安装语音包并立即触发一次声音，语音包状态会保存到实体数据。
- 红石脉冲使用每个服务端维度的实体红石索引维护：处于激活状态的叮咚鸡所在方块以及六个相邻方块可为红石线查询提供 15 级信号，多个实体位于同一位置时使用引用计数，实体移动、卸载或移除时只撤销自己的来源。
- 来源变化会通知实体所在位置和相邻接收位置，覆盖与真实红石方块共格、红石线缓存以及比较器更新；新增 `ServerLevel`/红石信号桥接，并在安装 Alternate Current 时额外适配其节点坐标查询，未安装时不加载可选兼容路径。
- 叮咚鸡渲染器在原版鸡模型背部附加钟体、钟锤和底座；钟体随每次脉冲摆动，选取范围向上扩展到可见钟的位置但不改变鸡的实体碰撞体。新增专用音效、字幕、掉落表和中英文资源。

## 大狗音波炮

- 新增大狗音波炮：使用装有狼的小型纸箱或大型纸箱、活体构件、齿轮、安山机壳、安山合金和安山桌面通过机械合成获得。它是一件耐久度 100 的双手蓄力远程装备，蓄力至少 10 tick、40 tick 达到满蓄力，发射后有 20 tick 冷却。
- 普通模式按蓄力程度将攻击范围从 4 格增长到 16 格，在约 120° 的扇形内命中除射手外的生物；目标会获得持续时间随距离从 5 秒衰减到 2 秒的眩晕，并受到基础击退，受击者在眩晕期间不能攻击、跳跃、使用物品、放置或破坏方块、交互实体和方块，也不能进行其他玩家交互。
- 眩晕效果会使移动速度降低 50%；客户端对被眩晕的玩家加入轻微的俯仰、偏航和滚转抖动，服务端事件拦截保证眩晕不是只有视觉效果。效果支持兼容 Cataclysm 的同名眩晕效果：该模组存在且效果已注册时优先复用，否则使用本模组的眩晕效果。
- 不安装改装时，蓄力过程播放狼叫；发射时播放普通狼声音。大狗音波炮可使用 Create 的铜背罐或下界合金背罐气压抵消耐久损耗，否则蓄力期间每 20 tick 消耗 1 点耐久；耐久条使用背罐兼容的显示逻辑。
- 新增四种锻造台改装，锻造模板槽留空，且每次改装保留大狗音波炮原有数据和已经安装的改装：音符盒安装语音包，玻璃板安装瞄准镜，染料安装/换色狗狗项圈，幽匿尖啸体安装尖啸音爆。
- 语音包增加蓄力起始、蓄力循环、满蓄力发射和部分蓄力发射声音；瞄准镜为外观改装，潜行右击可收起/展开；狗狗项圈按染料改变颜色；副手手持扳手并潜行右击可按安装逆序卸下最后一个改装，并返还音符盒、玻璃板或幽匿尖啸体等可返还材料。
- 尖啸音爆模式将普通眩晕改为原版音爆：范围从 8 格增长到 24 格，伤害从 6 增长到 12，并按音爆附魔等级每级额外增加 2 点伤害；使用音爆伤害源无视护甲，同时保留原版音爆的击退和附魔提供的额外击退。
- 大狗音波炮支持耐久、经验修补、消失诅咒、抢夺和冲击等相关附魔；新增最高 5 级的“音爆”宝藏附魔，可用于大狗音波炮，并加入远古城市战利品表。“音爆”附魔只提高尖啸音爆改装的伤害，不改变普通眩晕模式。
- 改装顺序以版本化数据组件保存，并兼容旧的紧凑编码；重复安装普通改装会被拒绝，狗狗项圈只有换成不同颜色时才会重新应用。所有安装/卸下操作在服务端完成，客户端通过同步数据更新名称、颜色、瞄准镜状态和改装列表。
- 客户端新增定制物品模型和双手持握姿势：蓄力时双手托住炮体，狼模型在未满蓄力和满蓄力间切换，爪子随蓄力轻微起伏，齿轮按蓄力加速并在发射后减速，尖啸音爆显示眼部效果，瞄准镜根据左右手和收起状态选择模型，狗狗项圈使用染料颜色着色。
- 发射数据由服务端计算并发送方向、射程和射手信息；客户端在正确的第一/第三人称炮口位置生成沿方向传播的自定义音波粒子，粒子半径随波前扩大、前后双面渲染并处理延迟，从而让不同视角看到的音波与实际命中方向一致。

## 三类传送带的整链架构与 Create 兼容

- 动力传送带、史莱姆传送带和岩浆怪传送带统一接入 `CBBeltChain`、`CBBeltChainBlock`、`CBBeltShapeSet`、`CBBeltTransform` 和 `CBBeltSlicer`。链遍历只接受同一具体传送带类型、同一逻辑空间中的相邻段，最多处理 1000 段，并明确区分完整、区块未加载、跨空间、类型无效和超长五种结果；遍历不会为了重建链而强制加载区块。
- 传送带链在区块未加载或跨 Sable 子空间时保留已有状态并等待，初始化失败后每 20 tick 重试；只有确认结构无效或超过长度上限时才拆除。渲染、碰撞和清理可以使用已加载的安全前缀，但库存、移动结构和运输重建只接受完整链，避免把半条链误当作可用设备。
- 新增共用的端点、滑轮、坡度、外壳和长度元数据。传送带放置物品按 Create 的特殊方块需求处理：端点消耗传动杆，中间段不重复消耗；动力传送带和岩浆怪传送带继续保存外壳状态，移动或复制时也保存滑轮偏移和每段外壳类型。
- 三类传送带的连接操作共用端点选择、坡度/朝向和链路生成逻辑；选择数据保存维度与子空间，并在跨空间、未加载、超距、失效或类型不符时清除或拒绝。传送带切割与合并也共用材料检查、消耗、翻转兼容和同空间校验，合并后主动失效旧能力缓存，避免出现只更新一端的链。
- 蓝图、蓝图加农炮（`Schematicannon`）、机械手（`Deployer`）和 Create 移动结构现在收集并重建完整传送带链，而不是把每一段当成独立方块。重建前检查所有位置、区块和逻辑空间，先快照、再原子放置端点/滑轮/传送带并恢复外壳；任一步骤失败都会按逆序回滚，避免只生成半条传送带或丢失原方块。移动链上的乘客、传送物品和链控制器关系也随整体结构保留。
- 传送带的水平、上坡、下坡、竖直、侧向几何、碰撞箱、选择箱、切割判定和 X/Z 轴变换统一由基础层计算；旋转或移动结构后，传送带段的方向、坡度和端点关系一起变换，不再因只变换方块朝向而留下无法连接的链。

## 史莱姆传送带回环与运输规则

- 史莱姆传送带最终采用闭合回环：末端把物品转入内部的 BACK 轨道，物品沿回环返回入口；FRONT 是对外的主运输面，BACK 只承担回环、末端掉落捕获和漏斗交互。轨道选择按掉落物与实际传送带表面的接触位置确定，通用物品能力和手动拾取仍固定走 FRONT，避免同一物品在两套接口中重复出现。
- 新的 `AdvancePass` 和插入规划器统一处理分段推进、入口/出口转弯、间距、占用探测、插入落点和阻塞状态；规划阶段与实际落位使用同一套结果，支持水平到竖直、侧向/转角以及传送带之间的连续交接，并在区块重载、运动方向反转后刷新实时方向。竖直和侧向状态不承载实体，但物品的有效运输路径仍按相应几何计算。
- 史莱姆传送带的普通物品能力、包裹右击、空手拾取、加工输入、粉碎轮、隧道、动力臂、显示链接器、智能侦测器、鼓风机和工作盆输出都限定在 FRONT；只有漏斗可以按实际接触面访问 FRONT 或 BACK。停止的史莱姆传送带仍可作为工作盆的有效输出目标，内部回环不会意外暴露成普通 Create 传送带。
- 补齐史莱姆传送带与安山隧道、黄铜隧道的连接、生存、窗口形状、左右分流、挡板、卡料和黄铜分配行为；下方传送带变化时主动失效旧能力缓存。岩浆怪传送带接入隧道时会根据是否装壳写入安山机壳或黄铜机壳状态，避免隧道能放置但无法正确识别输出。
- 史莱姆传送带新增水平粉碎轮输入、动力臂交互和传送带表面处理适配；普通 Create 的传送带、漏斗和隧道继续走原有路径。漏斗的方向、伸缩状态、过滤数量和能力目标会先解析自定义传送带表面，再回退到 Create 原逻辑，不把普通传送带误判为史莱姆回环。
- 动力传送带将实体表面移动采样和应力输出改为控制器级聚合：按游戏 tick 累积实体运动，在检测间隔取样、按四档舍入转速并计算输出能力，只有链控制器产生动力，避免每个传送带段重复施加应力或在实体数量变化时产生抖动。
- 三类传送带共享 Flywheel 可视对象、渲染状态和传送带辅助渲染；史莱姆回环的两面、岩浆怪传送带的热力状态、动力传送带的运动反馈以及端点/滑轮状态都会随链重建和方块实体同步刷新。

## 多方块机器生命周期与移动结构

- 新增 `CBMultiBlockLifecycle`，统一管理多方块机器的锚点、部件识别、延迟验证、掉落抑制和 Create 移动检查。邻居变化不再在 `updateShape` 中立即拆结构，而是只安排下一 tick 的服务端验证；区块未加载的位置视为暂时完整，避免跨区块结构因边界更新被误拆或强制读盘。
- 巨型青蛙、唤魔者附魔室、潜影传送站和蜘蛛装配台都使用同一套多方块生命周期。客户端与服务端放置部件保持一致以消除视觉幽灵方块；只有锚点产生方块掉落，其余部件静默移除；从任意部件破坏、爆炸或第三方 `destroyBlock` 触发时都不会重复掉落。
- 创造模式破坏非锚点部件时会移除锚点但不生成方块物品，同时保留原版方块破坏粒子和声音；潜行扳手拆除则先走可取消的破坏事件，再按机器规则只生成一份掉落。活塞和 Create 移动检查把同一逻辑机器作为一个整体处理，部件不会被单独吸附、遗留或在移动后立即因邻居暂未放置而消失。
- 巨型青蛙的渲染包围盒扩展到完整的 `3 × 3 × 2` 身体、舌头和传送带交接范围；嘴部部件补齐方块实体 tick 和行为初始化，主部件移除时仍会安全处理嘴部待交接物品。唤魔者附魔室的上半部不再创建多余的代理方块实体，管道能力改由方块级提供；其上下两格可作为整体旋转，且保持不可被活塞推动。
- 潜影传送站增加三格结构的高度、世界边界和放置检查，并让任意部件的渲染包围盒覆盖完整装置。装置卸载、移除或结构失效时会注销空间地址，避免已不存在的上部部件继续作为传送目标。蜘蛛装配台则加强本体与蜘蛛装配台尾齿轮的相互验证，齿轮位置错误或结构断开会延迟清理，库存也会在本体移除时按容器规则掉落。

## 纸箱中的生物数据与实体显示管线

- 纸箱捕获数据统一改为只读视图：`CBItemData` 同时服务于释放、替换、网络同步、配方判断和渲染预览。空内容纸箱仍保留 Create 包裹所需的虚拟自身内容，但悬浮提示、物品图标和 JEI 显示会过滤该回退内容，不把内部兼容数据显示给玩家。
- 创建捕获实体时增加跨服务端维度的 UUID 冲突检查；若实体或乘客树中的 UUID 已被占用，会为整棵乘客树重新生成 UUID，避免释放纸箱内容后出现重复实体、骑乘关系串接或存档覆盖。原型捕获数据与实际实例数据分开处理，原型图标不依赖当前世界。
- 新增渲染线程专用的 `CapturedEntityRenderManager`：实体实例、烘焙图标、失败结果和待处理任务均有上限与淘汰策略，实例缓存会在切换世界、退出登录和资源重载时清理。按世界、界面和手持物品的优先级分帧准备实体，限制同一帧准备数量，冻结待烘焙实体状态并在失败后延迟重试，降低大量纸箱同时打开时的卡顿。
- 捕获实体图标改为每个纸箱面独立裁剪并按 `RenderType` 分组的不可变网格，回放时使用当前光照；达到顶点上限时安全放弃，不再每帧重新遍历实体渲染层、裁剪几何和分配临时对象。纸箱实体渲染器只在 32 格范围内显示捕获生物，并按配置在物流纸箱模型和捕获模型之间切换。
- 物品栏、手持物品、方块实体和 JEI 统一使用缓存的实体显示器：支持方块中心、脚部锚定、固定/自动缩放、脚部偏移、Y 轴旋转和实际渲染顶点测量；测量失败时回退到实体尺寸。通用状态保存/恢复、步行动画、光照和嵌套渲染边界得到统一处理，避免一个图标的姿态或矩阵污染后续物品。
- 未完成的悦灵快递员、巨型青蛙物品、捕获的小型史莱姆和其他活体物品迁移到通用缓存实体渲染；悦灵快递员改为方块中心展示，旧的专用渲染器删除。纸箱实体图标、纸箱实体和 JEI 图标都受同一个客户端配置开关控制。

## JEI 配方、实体动画与显示兼容

- 重建 JEI 插件注册：新增史莱姆转化、生物手动物品使用、爆破室高压聚爆、鱿鱼打印机和唤魔者附魔室分类，并为传送液提供流体信息。爆破室核心同时作为 Create 粉碎和高压聚爆分类的催化物，鱿鱼打印机和唤魔者附魔室分别作为对应分类的催化物。
- 生物手动物品使用分类明确展示“鸡”使用“呼唤铃”转化为“叮咚鸡”，以及“叮咚鸡”使用“音符盒”安装“语音包”两条最终配方，并在分类动画中缓存鸡和叮咚鸡实体。大狗音波炮改装接入原版锻造台分类：模板槽留空，基础物品为大狗音波炮，支持语音包、瞄准镜、尖啸音爆和所有染料对应的狗狗项圈输出，并根据当前聚焦槽位显示匹配改装。
- JEI 场景粒子不再直接依赖游戏世界的粒子引擎，而是在场景内维护独立粒子列表，按原版渲染类型顺序逐 tick 更新并处理场景相机、姿态、光照、深度和裁剪；鱿鱼打印机墨水和大狗音波炮音波等自定义粒子在 JEI 场景中可以按正确方向显示。
- 统一 `AnimatedKineticsWithEntities` 作为 Create 动力场景与生物实体动画的基础，重构唤魔者附魔室、鱿鱼打印机、史莱姆转化、史莱姆/岩浆怪实体和黄油猫等已有分类的实体渲染与动力变换，减少不同分类各自维护矩阵和光照状态造成的偏差。
- JEI 槽位和布局注入兼容现代及旧版槽位绘制签名：有捕获生物的小型纸箱/大型纸箱在未悬停时显示居中的生物和大型纸箱标记，悬停时仍显示实际纸箱物品，并把当前槽位上下文传给实体图标渲染器。纸箱渲染关闭时完整回退到 JEI 原始物品绘制。

## 既有玩法修正、可选兼容与客户端细节

- 史莱姆培养皿保存并恢复 `MimicProfile`：记录的拟态生物在区块重载、客户端预览和最终生成时使用同一套外观/变种数据；培养皿实现 `Clearable`，清空时同步清除物品、流体和记录的拟态档案。固定的钓竿同样实现 `Clearable`，移除时会清理诱饵，避免方块实体内容残留。
- 爆破室核心实现延迟提取预览能力：标记中的苦力怕尚未完成异步处理时，下游仍可读取只读的输出预览；预览不会提前消费库存，真正取出仍由服务端处理事务完成。爆破室的已加载缓存和方块实体生命周期同步调整，避免预览引用卸载方块。
- 史莱姆万向节在已有端点互惠审计之外，达到配置的弹性断开距离时也会请求断裂；潜影传送站使用完整三格包围盒并在卸载时注销子空间地址，减少渲染和传送目标的悬空引用。
- 增加 Alternate Current 可选兼容：只有依赖类存在时才加载对应 Mixin；叮咚鸡实体红石来源会并入其节点查询，同时保留原版红石线的实体信号来源。未安装 Alternate Current 时，主模组不会触发其类加载或反射路径。
- 客户端冻结捕获实体烘焙时的 `partial tick`，防止同一图标在不同帧采样到不同姿态；史莱姆护甲层使用半透明渲染，拟态生物的外层渲染延后到身体替换完成后处理，避免纹理层覆盖错误。动力传送带行走动画、方块物品的实体姿态和嵌套渲染状态也统一经由新的辅助类恢复。
- 鱿鱼打印机补充独立墨水粒子类型和客户端粒子选项；墨水粒子带有落点限制，在低于鱿鱼打印机约 1.2 格的位置消失，避免穿过置物台继续下沉。巨型青蛙物品、纸箱和黄油猫的已有渲染器改用缓存实体/共享状态管线，减少方块物品、GUI 与世界渲染之间的比例和朝向差异。

## 配方、资源、构建与维护性

- 小型纸箱的切制产量从 2 个提高到 4 个；大型纸箱的切制配方明确从捆装纸板块产出 2 个大型纸箱。新增岩浆怪燃烧室捕获配方、叮咚鸡掉落表、大狗音波炮机械合成与四种锻造台改装配方，并加入“音爆”宝藏附魔、远古城市战利品和对应全局战利品修改器。
- 为岩浆怪燃烧室加入空/已捕获状态的方块状态、模型、纹理、掉落表和燃烧粒子；为叮咚鸡加入钟体模型、音效、语音包音频、实体/物品模型和掉落资源；为大狗音波炮加入狼、爪子、齿轮、瞄准镜、项圈和尖啸眼部模型，以及音波粒子、声音和中英文语言资源。史莱姆护甲层、墨水粒子和 Create 鼓风机加工标签也同步补齐。
- 网络协议版本从 15 提升到 21，并登记大狗音波炮发射、蓄力音效、齿轮动画和叮咚鸡语音包音效等客户端数据包，使这些状态在服务端计算、客户端表现和重连后同步一致。
- 版本依赖更新为 NeoForge 21.1.234，Create 版本范围收紧为 `[6.0.10,6.0.12)`，JEI 运行版本更新为 19.39.0.368 并声明兼容范围；Alternate Current 作为不传递的可选编译依赖加入。Sable、Create、JEI 和可选兼容的 Mixin 配置继续分离，缺少可选依赖时不会阻塞主模组加载。
- 构建脚本新增 `quickPlaySmoke`：可通过 `test.py --smoke` 进入世界、检查日志标记、按超时退出并清理客户端；同时支持 dry-run、超时和日志行数参数。README 中补充该验证入口，便于对传送带、多方块生命周期和客户端注册进行最小启动回归。
- 新增传送带 Create 对等性/修复计划和 Mixin 全量审阅文档，记录链遍历、隧道、漏斗、粉碎轮、动力臂、显示链接器、移动蓝图、可选依赖和映射风险；同步更新中英文 README、优化文档、资源清单和版本依赖说明。
